-- Generated Lua functions (2025-11-22T02:01:00Z)

function gen_func_1(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1
  end
  return sum + (a or 0)
end

function gen_func_2(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2, val = A + 2, ok = (b ~= nil) }
end

function gen_func_3(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 3 + B
end

function gen_func_4(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 4
  end
  return sum + (a or 0)
end

function gen_func_5(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 5, val = A + 5, ok = (b ~= nil) }
end

function gen_func_6(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 6 + B
end

function gen_func_7(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 7
  end
  return sum + (a or 0)
end

function gen_func_8(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 8, val = A + 8, ok = (b ~= nil) }
end

function gen_func_9(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 9 + B
end

function gen_func_10(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 10
  end
  return sum + (a or 0)
end

function gen_func_11(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 11, val = A + 1, ok = (b ~= nil) }
end

function gen_func_12(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 12 + B
end

function gen_func_13(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 13
  end
  return sum + (a or 0)
end

function gen_func_14(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 14, val = A + 4, ok = (b ~= nil) }
end

function gen_func_15(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 15 + B
end

function gen_func_16(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 16
  end
  return sum + (a or 0)
end

function gen_func_17(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 17, val = A + 7, ok = (b ~= nil) }
end

function gen_func_18(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 18 + B
end

function gen_func_19(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 19
  end
  return sum + (a or 0)
end

function gen_func_20(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 20, val = A + 0, ok = (b ~= nil) }
end

function gen_func_21(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 21 + B
end

function gen_func_22(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 22
  end
  return sum + (a or 0)
end

function gen_func_23(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 23, val = A + 3, ok = (b ~= nil) }
end

function gen_func_24(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 24 + B
end

function gen_func_25(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 25
  end
  return sum + (a or 0)
end

function gen_func_26(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 26, val = A + 6, ok = (b ~= nil) }
end

function gen_func_27(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 27 + B
end

function gen_func_28(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 28
  end
  return sum + (a or 0)
end

function gen_func_29(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 29, val = A + 9, ok = (b ~= nil) }
end

function gen_func_30(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 30 + B
end

function gen_func_31(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 31
  end
  return sum + (a or 0)
end

function gen_func_32(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 32, val = A + 2, ok = (b ~= nil) }
end

function gen_func_33(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 33 + B
end

function gen_func_34(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 34
  end
  return sum + (a or 0)
end

function gen_func_35(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 35, val = A + 5, ok = (b ~= nil) }
end

function gen_func_36(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 36 + B
end

function gen_func_37(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 37
  end
  return sum + (a or 0)
end

function gen_func_38(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 38, val = A + 8, ok = (b ~= nil) }
end

function gen_func_39(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 39 + B
end

function gen_func_40(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 40
  end
  return sum + (a or 0)
end

function gen_func_41(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 41, val = A + 1, ok = (b ~= nil) }
end

function gen_func_42(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 42 + B
end

function gen_func_43(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 43
  end
  return sum + (a or 0)
end

function gen_func_44(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 44, val = A + 4, ok = (b ~= nil) }
end

function gen_func_45(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 45 + B
end

function gen_func_46(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 46
  end
  return sum + (a or 0)
end

function gen_func_47(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 47, val = A + 7, ok = (b ~= nil) }
end

function gen_func_48(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 48 + B
end

function gen_func_49(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 49
  end
  return sum + (a or 0)
end

function gen_func_50(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 50, val = A + 0, ok = (b ~= nil) }
end

function gen_func_51(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 51 + B
end

function gen_func_52(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 52
  end
  return sum + (a or 0)
end

function gen_func_53(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 53, val = A + 3, ok = (b ~= nil) }
end

function gen_func_54(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 54 + B
end

function gen_func_55(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 55
  end
  return sum + (a or 0)
end

function gen_func_56(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 56, val = A + 6, ok = (b ~= nil) }
end

function gen_func_57(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 57 + B
end

function gen_func_58(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 58
  end
  return sum + (a or 0)
end

function gen_func_59(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 59, val = A + 9, ok = (b ~= nil) }
end

function gen_func_60(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 60 + B
end

function gen_func_61(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 61
  end
  return sum + (a or 0)
end

function gen_func_62(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 62, val = A + 2, ok = (b ~= nil) }
end

function gen_func_63(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 63 + B
end

function gen_func_64(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 64
  end
  return sum + (a or 0)
end

function gen_func_65(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 65, val = A + 5, ok = (b ~= nil) }
end

function gen_func_66(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 66 + B
end

function gen_func_67(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 67
  end
  return sum + (a or 0)
end

function gen_func_68(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 68, val = A + 8, ok = (b ~= nil) }
end

function gen_func_69(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 69 + B
end

function gen_func_70(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 70
  end
  return sum + (a or 0)
end

function gen_func_71(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 71, val = A + 1, ok = (b ~= nil) }
end

function gen_func_72(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 72 + B
end

function gen_func_73(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 73
  end
  return sum + (a or 0)
end

function gen_func_74(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 74, val = A + 4, ok = (b ~= nil) }
end

function gen_func_75(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 75 + B
end

function gen_func_76(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 76
  end
  return sum + (a or 0)
end

function gen_func_77(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 77, val = A + 7, ok = (b ~= nil) }
end

function gen_func_78(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 78 + B
end

function gen_func_79(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 79
  end
  return sum + (a or 0)
end

function gen_func_80(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 80, val = A + 0, ok = (b ~= nil) }
end

function gen_func_81(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 81 + B
end

function gen_func_82(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 82
  end
  return sum + (a or 0)
end

function gen_func_83(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 83, val = A + 3, ok = (b ~= nil) }
end

function gen_func_84(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 84 + B
end

function gen_func_85(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 85
  end
  return sum + (a or 0)
end

function gen_func_86(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 86, val = A + 6, ok = (b ~= nil) }
end

function gen_func_87(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 87 + B
end

function gen_func_88(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 88
  end
  return sum + (a or 0)
end

function gen_func_89(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 89, val = A + 9, ok = (b ~= nil) }
end

function gen_func_90(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 90 + B
end

function gen_func_91(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 91
  end
  return sum + (a or 0)
end

function gen_func_92(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 92, val = A + 2, ok = (b ~= nil) }
end

function gen_func_93(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 93 + B
end

function gen_func_94(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 94
  end
  return sum + (a or 0)
end

function gen_func_95(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 95, val = A + 5, ok = (b ~= nil) }
end

function gen_func_96(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 96 + B
end

function gen_func_97(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 97
  end
  return sum + (a or 0)
end

function gen_func_98(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 98, val = A + 8, ok = (b ~= nil) }
end

function gen_func_99(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 99 + B
end

function gen_func_100(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 100
  end
  return sum + (a or 0)
end

function gen_func_101(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 101, val = A + 1, ok = (b ~= nil) }
end

function gen_func_102(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 102 + B
end

function gen_func_103(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 103
  end
  return sum + (a or 0)
end

function gen_func_104(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 104, val = A + 4, ok = (b ~= nil) }
end

function gen_func_105(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 105 + B
end

function gen_func_106(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 106
  end
  return sum + (a or 0)
end

function gen_func_107(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 107, val = A + 7, ok = (b ~= nil) }
end

function gen_func_108(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 108 + B
end

function gen_func_109(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 109
  end
  return sum + (a or 0)
end

function gen_func_110(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 110, val = A + 0, ok = (b ~= nil) }
end

function gen_func_111(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 111 + B
end

function gen_func_112(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 112
  end
  return sum + (a or 0)
end

function gen_func_113(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 113, val = A + 3, ok = (b ~= nil) }
end

function gen_func_114(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 114 + B
end

function gen_func_115(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 115
  end
  return sum + (a or 0)
end

function gen_func_116(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 116, val = A + 6, ok = (b ~= nil) }
end

function gen_func_117(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 117 + B
end

function gen_func_118(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 118
  end
  return sum + (a or 0)
end

function gen_func_119(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 119, val = A + 9, ok = (b ~= nil) }
end

function gen_func_120(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 120 + B
end

function gen_func_121(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 121
  end
  return sum + (a or 0)
end

function gen_func_122(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 122, val = A + 2, ok = (b ~= nil) }
end

function gen_func_123(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 123 + B
end

function gen_func_124(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 124
  end
  return sum + (a or 0)
end

function gen_func_125(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 125, val = A + 5, ok = (b ~= nil) }
end

function gen_func_126(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 126 + B
end

function gen_func_127(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 127
  end
  return sum + (a or 0)
end

function gen_func_128(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 128, val = A + 8, ok = (b ~= nil) }
end

function gen_func_129(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 129 + B
end

function gen_func_130(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 130
  end
  return sum + (a or 0)
end

function gen_func_131(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 131, val = A + 1, ok = (b ~= nil) }
end

function gen_func_132(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 132 + B
end

function gen_func_133(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 133
  end
  return sum + (a or 0)
end

function gen_func_134(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 134, val = A + 4, ok = (b ~= nil) }
end

function gen_func_135(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 135 + B
end

function gen_func_136(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 136
  end
  return sum + (a or 0)
end

function gen_func_137(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 137, val = A + 7, ok = (b ~= nil) }
end

function gen_func_138(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 138 + B
end

function gen_func_139(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 139
  end
  return sum + (a or 0)
end

function gen_func_140(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 140, val = A + 0, ok = (b ~= nil) }
end

function gen_func_141(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 141 + B
end

function gen_func_142(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 142
  end
  return sum + (a or 0)
end

function gen_func_143(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 143, val = A + 3, ok = (b ~= nil) }
end

function gen_func_144(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 144 + B
end

function gen_func_145(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 145
  end
  return sum + (a or 0)
end

function gen_func_146(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 146, val = A + 6, ok = (b ~= nil) }
end

function gen_func_147(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 147 + B
end

function gen_func_148(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 148
  end
  return sum + (a or 0)
end

function gen_func_149(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 149, val = A + 9, ok = (b ~= nil) }
end

function gen_func_150(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 150 + B
end

function gen_func_151(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 151
  end
  return sum + (a or 0)
end

function gen_func_152(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 152, val = A + 2, ok = (b ~= nil) }
end

function gen_func_153(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 153 + B
end

function gen_func_154(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 154
  end
  return sum + (a or 0)
end

function gen_func_155(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 155, val = A + 5, ok = (b ~= nil) }
end

function gen_func_156(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 156 + B
end

function gen_func_157(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 157
  end
  return sum + (a or 0)
end

function gen_func_158(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 158, val = A + 8, ok = (b ~= nil) }
end

function gen_func_159(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 159 + B
end

function gen_func_160(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 160
  end
  return sum + (a or 0)
end

function gen_func_161(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 161, val = A + 1, ok = (b ~= nil) }
end

function gen_func_162(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 162 + B
end

function gen_func_163(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 163
  end
  return sum + (a or 0)
end

function gen_func_164(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 164, val = A + 4, ok = (b ~= nil) }
end

function gen_func_165(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 165 + B
end

function gen_func_166(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 166
  end
  return sum + (a or 0)
end

function gen_func_167(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 167, val = A + 7, ok = (b ~= nil) }
end

function gen_func_168(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 168 + B
end

function gen_func_169(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 169
  end
  return sum + (a or 0)
end

function gen_func_170(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 170, val = A + 0, ok = (b ~= nil) }
end

function gen_func_171(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 171 + B
end

function gen_func_172(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 172
  end
  return sum + (a or 0)
end

function gen_func_173(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 173, val = A + 3, ok = (b ~= nil) }
end

function gen_func_174(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 174 + B
end

function gen_func_175(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 175
  end
  return sum + (a or 0)
end

function gen_func_176(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 176, val = A + 6, ok = (b ~= nil) }
end

function gen_func_177(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 177 + B
end

function gen_func_178(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 178
  end
  return sum + (a or 0)
end

function gen_func_179(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 179, val = A + 9, ok = (b ~= nil) }
end

function gen_func_180(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 180 + B
end

function gen_func_181(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 181
  end
  return sum + (a or 0)
end

function gen_func_182(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 182, val = A + 2, ok = (b ~= nil) }
end

function gen_func_183(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 183 + B
end

function gen_func_184(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 184
  end
  return sum + (a or 0)
end

function gen_func_185(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 185, val = A + 5, ok = (b ~= nil) }
end

function gen_func_186(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 186 + B
end

function gen_func_187(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 187
  end
  return sum + (a or 0)
end

function gen_func_188(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 188, val = A + 8, ok = (b ~= nil) }
end

function gen_func_189(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 189 + B
end

function gen_func_190(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 190
  end
  return sum + (a or 0)
end

function gen_func_191(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 191, val = A + 1, ok = (b ~= nil) }
end

function gen_func_192(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 192 + B
end

function gen_func_193(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 193
  end
  return sum + (a or 0)
end

function gen_func_194(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 194, val = A + 4, ok = (b ~= nil) }
end

function gen_func_195(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 195 + B
end

function gen_func_196(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 196
  end
  return sum + (a or 0)
end

function gen_func_197(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 197, val = A + 7, ok = (b ~= nil) }
end

function gen_func_198(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 198 + B
end

function gen_func_199(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 199
  end
  return sum + (a or 0)
end

function gen_func_200(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 200, val = A + 0, ok = (b ~= nil) }
end

function gen_func_201(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 201 + B
end

function gen_func_202(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 202
  end
  return sum + (a or 0)
end

function gen_func_203(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 203, val = A + 3, ok = (b ~= nil) }
end

function gen_func_204(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 204 + B
end

function gen_func_205(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 205
  end
  return sum + (a or 0)
end

function gen_func_206(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 206, val = A + 6, ok = (b ~= nil) }
end

function gen_func_207(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 207 + B
end

function gen_func_208(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 208
  end
  return sum + (a or 0)
end

function gen_func_209(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 209, val = A + 9, ok = (b ~= nil) }
end

function gen_func_210(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 210 + B
end

function gen_func_211(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 211
  end
  return sum + (a or 0)
end

function gen_func_212(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 212, val = A + 2, ok = (b ~= nil) }
end

function gen_func_213(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 213 + B
end

function gen_func_214(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 214
  end
  return sum + (a or 0)
end

function gen_func_215(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 215, val = A + 5, ok = (b ~= nil) }
end

function gen_func_216(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 216 + B
end

function gen_func_217(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 217
  end
  return sum + (a or 0)
end

function gen_func_218(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 218, val = A + 8, ok = (b ~= nil) }
end

function gen_func_219(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 219 + B
end

function gen_func_220(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 220
  end
  return sum + (a or 0)
end

function gen_func_221(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 221, val = A + 1, ok = (b ~= nil) }
end

function gen_func_222(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 222 + B
end

function gen_func_223(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 223
  end
  return sum + (a or 0)
end

function gen_func_224(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 224, val = A + 4, ok = (b ~= nil) }
end

function gen_func_225(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 225 + B
end

function gen_func_226(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 226
  end
  return sum + (a or 0)
end

function gen_func_227(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 227, val = A + 7, ok = (b ~= nil) }
end

function gen_func_228(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 228 + B
end

function gen_func_229(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 229
  end
  return sum + (a or 0)
end

function gen_func_230(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 230, val = A + 0, ok = (b ~= nil) }
end

function gen_func_231(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 231 + B
end

function gen_func_232(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 232
  end
  return sum + (a or 0)
end

function gen_func_233(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 233, val = A + 3, ok = (b ~= nil) }
end

function gen_func_234(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 234 + B
end

function gen_func_235(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 235
  end
  return sum + (a or 0)
end

function gen_func_236(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 236, val = A + 6, ok = (b ~= nil) }
end

function gen_func_237(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 237 + B
end

function gen_func_238(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 238
  end
  return sum + (a or 0)
end

function gen_func_239(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 239, val = A + 9, ok = (b ~= nil) }
end

function gen_func_240(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 240 + B
end

function gen_func_241(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 241
  end
  return sum + (a or 0)
end

function gen_func_242(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 242, val = A + 2, ok = (b ~= nil) }
end

function gen_func_243(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 243 + B
end

function gen_func_244(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 244
  end
  return sum + (a or 0)
end

function gen_func_245(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 245, val = A + 5, ok = (b ~= nil) }
end

function gen_func_246(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 246 + B
end

function gen_func_247(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 247
  end
  return sum + (a or 0)
end

function gen_func_248(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 248, val = A + 8, ok = (b ~= nil) }
end

function gen_func_249(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 249 + B
end

function gen_func_250(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 250
  end
  return sum + (a or 0)
end

function gen_func_251(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 251, val = A + 1, ok = (b ~= nil) }
end

function gen_func_252(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 252 + B
end

function gen_func_253(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 253
  end
  return sum + (a or 0)
end

function gen_func_254(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 254, val = A + 4, ok = (b ~= nil) }
end

function gen_func_255(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 255 + B
end

function gen_func_256(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 256
  end
  return sum + (a or 0)
end

function gen_func_257(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 257, val = A + 7, ok = (b ~= nil) }
end

function gen_func_258(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 258 + B
end

function gen_func_259(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 259
  end
  return sum + (a or 0)
end

function gen_func_260(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 260, val = A + 0, ok = (b ~= nil) }
end

function gen_func_261(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 261 + B
end

function gen_func_262(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 262
  end
  return sum + (a or 0)
end

function gen_func_263(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 263, val = A + 3, ok = (b ~= nil) }
end

function gen_func_264(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 264 + B
end

function gen_func_265(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 265
  end
  return sum + (a or 0)
end

function gen_func_266(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 266, val = A + 6, ok = (b ~= nil) }
end

function gen_func_267(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 267 + B
end

function gen_func_268(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 268
  end
  return sum + (a or 0)
end

function gen_func_269(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 269, val = A + 9, ok = (b ~= nil) }
end

function gen_func_270(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 270 + B
end

function gen_func_271(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 271
  end
  return sum + (a or 0)
end

function gen_func_272(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 272, val = A + 2, ok = (b ~= nil) }
end

function gen_func_273(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 273 + B
end

function gen_func_274(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 274
  end
  return sum + (a or 0)
end

function gen_func_275(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 275, val = A + 5, ok = (b ~= nil) }
end

function gen_func_276(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 276 + B
end

function gen_func_277(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 277
  end
  return sum + (a or 0)
end

function gen_func_278(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 278, val = A + 8, ok = (b ~= nil) }
end

function gen_func_279(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 279 + B
end

function gen_func_280(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 280
  end
  return sum + (a or 0)
end

function gen_func_281(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 281, val = A + 1, ok = (b ~= nil) }
end

function gen_func_282(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 282 + B
end

function gen_func_283(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 283
  end
  return sum + (a or 0)
end

function gen_func_284(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 284, val = A + 4, ok = (b ~= nil) }
end

function gen_func_285(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 285 + B
end

function gen_func_286(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 286
  end
  return sum + (a or 0)
end

function gen_func_287(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 287, val = A + 7, ok = (b ~= nil) }
end

function gen_func_288(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 288 + B
end

function gen_func_289(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 289
  end
  return sum + (a or 0)
end

function gen_func_290(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 290, val = A + 0, ok = (b ~= nil) }
end

function gen_func_291(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 291 + B
end

function gen_func_292(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 292
  end
  return sum + (a or 0)
end

function gen_func_293(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 293, val = A + 3, ok = (b ~= nil) }
end

function gen_func_294(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 294 + B
end

function gen_func_295(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 295
  end
  return sum + (a or 0)
end

function gen_func_296(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 296, val = A + 6, ok = (b ~= nil) }
end

function gen_func_297(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 297 + B
end

function gen_func_298(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 298
  end
  return sum + (a or 0)
end

function gen_func_299(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 299, val = A + 9, ok = (b ~= nil) }
end

function gen_func_300(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 300 + B
end

function gen_func_301(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 301
  end
  return sum + (a or 0)
end

function gen_func_302(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 302, val = A + 2, ok = (b ~= nil) }
end

function gen_func_303(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 303 + B
end

function gen_func_304(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 304
  end
  return sum + (a or 0)
end

function gen_func_305(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 305, val = A + 5, ok = (b ~= nil) }
end

function gen_func_306(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 306 + B
end

function gen_func_307(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 307
  end
  return sum + (a or 0)
end

function gen_func_308(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 308, val = A + 8, ok = (b ~= nil) }
end

function gen_func_309(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 309 + B
end

function gen_func_310(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 310
  end
  return sum + (a or 0)
end

function gen_func_311(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 311, val = A + 1, ok = (b ~= nil) }
end

function gen_func_312(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 312 + B
end

function gen_func_313(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 313
  end
  return sum + (a or 0)
end

function gen_func_314(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 314, val = A + 4, ok = (b ~= nil) }
end

function gen_func_315(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 315 + B
end

function gen_func_316(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 316
  end
  return sum + (a or 0)
end

function gen_func_317(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 317, val = A + 7, ok = (b ~= nil) }
end

function gen_func_318(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 318 + B
end

function gen_func_319(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 319
  end
  return sum + (a or 0)
end

function gen_func_320(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 320, val = A + 0, ok = (b ~= nil) }
end

function gen_func_321(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 321 + B
end

function gen_func_322(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 322
  end
  return sum + (a or 0)
end

function gen_func_323(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 323, val = A + 3, ok = (b ~= nil) }
end

function gen_func_324(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 324 + B
end

function gen_func_325(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 325
  end
  return sum + (a or 0)
end

function gen_func_326(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 326, val = A + 6, ok = (b ~= nil) }
end

function gen_func_327(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 327 + B
end

function gen_func_328(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 328
  end
  return sum + (a or 0)
end

function gen_func_329(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 329, val = A + 9, ok = (b ~= nil) }
end

function gen_func_330(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 330 + B
end

function gen_func_331(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 331
  end
  return sum + (a or 0)
end

function gen_func_332(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 332, val = A + 2, ok = (b ~= nil) }
end

function gen_func_333(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 333 + B
end

function gen_func_334(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 334
  end
  return sum + (a or 0)
end

function gen_func_335(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 335, val = A + 5, ok = (b ~= nil) }
end

function gen_func_336(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 336 + B
end

function gen_func_337(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 337
  end
  return sum + (a or 0)
end

function gen_func_338(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 338, val = A + 8, ok = (b ~= nil) }
end

function gen_func_339(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 339 + B
end

function gen_func_340(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 340
  end
  return sum + (a or 0)
end

function gen_func_341(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 341, val = A + 1, ok = (b ~= nil) }
end

function gen_func_342(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 342 + B
end

function gen_func_343(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 343
  end
  return sum + (a or 0)
end

function gen_func_344(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 344, val = A + 4, ok = (b ~= nil) }
end

function gen_func_345(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 345 + B
end

function gen_func_346(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 346
  end
  return sum + (a or 0)
end

function gen_func_347(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 347, val = A + 7, ok = (b ~= nil) }
end

function gen_func_348(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 348 + B
end

function gen_func_349(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 349
  end
  return sum + (a or 0)
end

function gen_func_350(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 350, val = A + 0, ok = (b ~= nil) }
end

function gen_func_351(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 351 + B
end

function gen_func_352(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 352
  end
  return sum + (a or 0)
end

function gen_func_353(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 353, val = A + 3, ok = (b ~= nil) }
end

function gen_func_354(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 354 + B
end

function gen_func_355(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 355
  end
  return sum + (a or 0)
end

function gen_func_356(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 356, val = A + 6, ok = (b ~= nil) }
end

function gen_func_357(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 357 + B
end

function gen_func_358(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 358
  end
  return sum + (a or 0)
end

function gen_func_359(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 359, val = A + 9, ok = (b ~= nil) }
end

function gen_func_360(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 360 + B
end

function gen_func_361(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 361
  end
  return sum + (a or 0)
end

function gen_func_362(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 362, val = A + 2, ok = (b ~= nil) }
end

function gen_func_363(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 363 + B
end

function gen_func_364(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 364
  end
  return sum + (a or 0)
end

function gen_func_365(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 365, val = A + 5, ok = (b ~= nil) }
end

function gen_func_366(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 366 + B
end

function gen_func_367(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 367
  end
  return sum + (a or 0)
end

function gen_func_368(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 368, val = A + 8, ok = (b ~= nil) }
end

function gen_func_369(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 369 + B
end

function gen_func_370(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 370
  end
  return sum + (a or 0)
end

function gen_func_371(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 371, val = A + 1, ok = (b ~= nil) }
end

function gen_func_372(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 372 + B
end

function gen_func_373(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 373
  end
  return sum + (a or 0)
end

function gen_func_374(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 374, val = A + 4, ok = (b ~= nil) }
end

function gen_func_375(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 375 + B
end

function gen_func_376(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 376
  end
  return sum + (a or 0)
end

function gen_func_377(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 377, val = A + 7, ok = (b ~= nil) }
end

function gen_func_378(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 378 + B
end

function gen_func_379(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 379
  end
  return sum + (a or 0)
end

function gen_func_380(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 380, val = A + 0, ok = (b ~= nil) }
end

function gen_func_381(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 381 + B
end

function gen_func_382(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 382
  end
  return sum + (a or 0)
end

function gen_func_383(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 383, val = A + 3, ok = (b ~= nil) }
end

function gen_func_384(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 384 + B
end

function gen_func_385(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 385
  end
  return sum + (a or 0)
end

function gen_func_386(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 386, val = A + 6, ok = (b ~= nil) }
end

function gen_func_387(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 387 + B
end

function gen_func_388(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 388
  end
  return sum + (a or 0)
end

function gen_func_389(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 389, val = A + 9, ok = (b ~= nil) }
end

function gen_func_390(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 390 + B
end

function gen_func_391(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 391
  end
  return sum + (a or 0)
end

function gen_func_392(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 392, val = A + 2, ok = (b ~= nil) }
end

function gen_func_393(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 393 + B
end

function gen_func_394(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 394
  end
  return sum + (a or 0)
end

function gen_func_395(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 395, val = A + 5, ok = (b ~= nil) }
end

function gen_func_396(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 396 + B
end

function gen_func_397(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 397
  end
  return sum + (a or 0)
end

function gen_func_398(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 398, val = A + 8, ok = (b ~= nil) }
end

function gen_func_399(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 399 + B
end

function gen_func_400(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 400
  end
  return sum + (a or 0)
end

function gen_func_401(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 401, val = A + 1, ok = (b ~= nil) }
end

function gen_func_402(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 402 + B
end

function gen_func_403(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 403
  end
  return sum + (a or 0)
end

function gen_func_404(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 404, val = A + 4, ok = (b ~= nil) }
end

function gen_func_405(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 405 + B
end

function gen_func_406(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 406
  end
  return sum + (a or 0)
end

function gen_func_407(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 407, val = A + 7, ok = (b ~= nil) }
end

function gen_func_408(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 408 + B
end

function gen_func_409(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 409
  end
  return sum + (a or 0)
end

function gen_func_410(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 410, val = A + 0, ok = (b ~= nil) }
end

function gen_func_411(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 411 + B
end

function gen_func_412(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 412
  end
  return sum + (a or 0)
end

function gen_func_413(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 413, val = A + 3, ok = (b ~= nil) }
end

function gen_func_414(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 414 + B
end

function gen_func_415(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 415
  end
  return sum + (a or 0)
end

function gen_func_416(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 416, val = A + 6, ok = (b ~= nil) }
end

function gen_func_417(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 417 + B
end

function gen_func_418(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 418
  end
  return sum + (a or 0)
end

function gen_func_419(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 419, val = A + 9, ok = (b ~= nil) }
end

function gen_func_420(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 420 + B
end

function gen_func_421(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 421
  end
  return sum + (a or 0)
end

function gen_func_422(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 422, val = A + 2, ok = (b ~= nil) }
end

function gen_func_423(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 423 + B
end

function gen_func_424(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 424
  end
  return sum + (a or 0)
end

function gen_func_425(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 425, val = A + 5, ok = (b ~= nil) }
end

function gen_func_426(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 426 + B
end

function gen_func_427(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 427
  end
  return sum + (a or 0)
end

function gen_func_428(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 428, val = A + 8, ok = (b ~= nil) }
end

function gen_func_429(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 429 + B
end

function gen_func_430(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 430
  end
  return sum + (a or 0)
end

function gen_func_431(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 431, val = A + 1, ok = (b ~= nil) }
end

function gen_func_432(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 432 + B
end

function gen_func_433(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 433
  end
  return sum + (a or 0)
end

function gen_func_434(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 434, val = A + 4, ok = (b ~= nil) }
end

function gen_func_435(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 435 + B
end

function gen_func_436(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 436
  end
  return sum + (a or 0)
end

function gen_func_437(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 437, val = A + 7, ok = (b ~= nil) }
end

function gen_func_438(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 438 + B
end

function gen_func_439(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 439
  end
  return sum + (a or 0)
end

function gen_func_440(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 440, val = A + 0, ok = (b ~= nil) }
end

function gen_func_441(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 441 + B
end

function gen_func_442(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 442
  end
  return sum + (a or 0)
end

function gen_func_443(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 443, val = A + 3, ok = (b ~= nil) }
end

function gen_func_444(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 444 + B
end

function gen_func_445(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 445
  end
  return sum + (a or 0)
end

function gen_func_446(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 446, val = A + 6, ok = (b ~= nil) }
end

function gen_func_447(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 447 + B
end

function gen_func_448(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 448
  end
  return sum + (a or 0)
end

function gen_func_449(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 449, val = A + 9, ok = (b ~= nil) }
end

function gen_func_450(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 450 + B
end

function gen_func_451(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 451
  end
  return sum + (a or 0)
end

function gen_func_452(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 452, val = A + 2, ok = (b ~= nil) }
end

function gen_func_453(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 453 + B
end

function gen_func_454(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 454
  end
  return sum + (a or 0)
end

function gen_func_455(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 455, val = A + 5, ok = (b ~= nil) }
end

function gen_func_456(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 456 + B
end

function gen_func_457(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 457
  end
  return sum + (a or 0)
end

function gen_func_458(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 458, val = A + 8, ok = (b ~= nil) }
end

function gen_func_459(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 459 + B
end

function gen_func_460(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 460
  end
  return sum + (a or 0)
end

function gen_func_461(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 461, val = A + 1, ok = (b ~= nil) }
end

function gen_func_462(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 462 + B
end

function gen_func_463(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 463
  end
  return sum + (a or 0)
end

function gen_func_464(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 464, val = A + 4, ok = (b ~= nil) }
end

function gen_func_465(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 465 + B
end

function gen_func_466(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 466
  end
  return sum + (a or 0)
end

function gen_func_467(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 467, val = A + 7, ok = (b ~= nil) }
end

function gen_func_468(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 468 + B
end

function gen_func_469(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 469
  end
  return sum + (a or 0)
end

function gen_func_470(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 470, val = A + 0, ok = (b ~= nil) }
end

function gen_func_471(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 471 + B
end

function gen_func_472(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 472
  end
  return sum + (a or 0)
end

function gen_func_473(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 473, val = A + 3, ok = (b ~= nil) }
end

function gen_func_474(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 474 + B
end

function gen_func_475(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 475
  end
  return sum + (a or 0)
end

function gen_func_476(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 476, val = A + 6, ok = (b ~= nil) }
end

function gen_func_477(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 477 + B
end

function gen_func_478(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 478
  end
  return sum + (a or 0)
end

function gen_func_479(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 479, val = A + 9, ok = (b ~= nil) }
end

function gen_func_480(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 480 + B
end

function gen_func_481(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 481
  end
  return sum + (a or 0)
end

function gen_func_482(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 482, val = A + 2, ok = (b ~= nil) }
end

function gen_func_483(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 483 + B
end

function gen_func_484(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 484
  end
  return sum + (a or 0)
end

function gen_func_485(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 485, val = A + 5, ok = (b ~= nil) }
end

function gen_func_486(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 486 + B
end

function gen_func_487(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 487
  end
  return sum + (a or 0)
end

function gen_func_488(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 488, val = A + 8, ok = (b ~= nil) }
end

function gen_func_489(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 489 + B
end

function gen_func_490(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 490
  end
  return sum + (a or 0)
end

function gen_func_491(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 491, val = A + 1, ok = (b ~= nil) }
end

function gen_func_492(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 492 + B
end

function gen_func_493(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 493
  end
  return sum + (a or 0)
end

function gen_func_494(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 494, val = A + 4, ok = (b ~= nil) }
end

function gen_func_495(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 495 + B
end

function gen_func_496(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 496
  end
  return sum + (a or 0)
end

function gen_func_497(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 497, val = A + 7, ok = (b ~= nil) }
end

function gen_func_498(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 498 + B
end

function gen_func_499(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 499
  end
  return sum + (a or 0)
end

function gen_func_500(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 500, val = A + 0, ok = (b ~= nil) }
end

function gen_func_501(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 501 + B
end

function gen_func_502(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 502
  end
  return sum + (a or 0)
end

function gen_func_503(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 503, val = A + 3, ok = (b ~= nil) }
end

function gen_func_504(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 504 + B
end

function gen_func_505(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 505
  end
  return sum + (a or 0)
end

function gen_func_506(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 506, val = A + 6, ok = (b ~= nil) }
end

function gen_func_507(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 507 + B
end

function gen_func_508(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 508
  end
  return sum + (a or 0)
end

function gen_func_509(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 509, val = A + 9, ok = (b ~= nil) }
end

function gen_func_510(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 510 + B
end

function gen_func_511(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 511
  end
  return sum + (a or 0)
end

function gen_func_512(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 512, val = A + 2, ok = (b ~= nil) }
end

function gen_func_513(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 513 + B
end

function gen_func_514(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 514
  end
  return sum + (a or 0)
end

function gen_func_515(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 515, val = A + 5, ok = (b ~= nil) }
end

function gen_func_516(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 516 + B
end

function gen_func_517(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 517
  end
  return sum + (a or 0)
end

function gen_func_518(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 518, val = A + 8, ok = (b ~= nil) }
end

function gen_func_519(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 519 + B
end

function gen_func_520(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 520
  end
  return sum + (a or 0)
end

function gen_func_521(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 521, val = A + 1, ok = (b ~= nil) }
end

function gen_func_522(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 522 + B
end

function gen_func_523(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 523
  end
  return sum + (a or 0)
end

function gen_func_524(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 524, val = A + 4, ok = (b ~= nil) }
end

function gen_func_525(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 525 + B
end

function gen_func_526(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 526
  end
  return sum + (a or 0)
end

function gen_func_527(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 527, val = A + 7, ok = (b ~= nil) }
end

function gen_func_528(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 528 + B
end

function gen_func_529(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 529
  end
  return sum + (a or 0)
end

function gen_func_530(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 530, val = A + 0, ok = (b ~= nil) }
end

function gen_func_531(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 531 + B
end

function gen_func_532(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 532
  end
  return sum + (a or 0)
end

function gen_func_533(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 533, val = A + 3, ok = (b ~= nil) }
end

function gen_func_534(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 534 + B
end

function gen_func_535(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 535
  end
  return sum + (a or 0)
end

function gen_func_536(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 536, val = A + 6, ok = (b ~= nil) }
end

function gen_func_537(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 537 + B
end

function gen_func_538(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 538
  end
  return sum + (a or 0)
end

function gen_func_539(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 539, val = A + 9, ok = (b ~= nil) }
end

function gen_func_540(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 540 + B
end

function gen_func_541(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 541
  end
  return sum + (a or 0)
end

function gen_func_542(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 542, val = A + 2, ok = (b ~= nil) }
end

function gen_func_543(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 543 + B
end

function gen_func_544(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 544
  end
  return sum + (a or 0)
end

function gen_func_545(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 545, val = A + 5, ok = (b ~= nil) }
end

function gen_func_546(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 546 + B
end

function gen_func_547(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 547
  end
  return sum + (a or 0)
end

function gen_func_548(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 548, val = A + 8, ok = (b ~= nil) }
end

function gen_func_549(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 549 + B
end

function gen_func_550(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 550
  end
  return sum + (a or 0)
end

function gen_func_551(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 551, val = A + 1, ok = (b ~= nil) }
end

function gen_func_552(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 552 + B
end

function gen_func_553(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 553
  end
  return sum + (a or 0)
end

function gen_func_554(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 554, val = A + 4, ok = (b ~= nil) }
end

function gen_func_555(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 555 + B
end

function gen_func_556(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 556
  end
  return sum + (a or 0)
end

function gen_func_557(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 557, val = A + 7, ok = (b ~= nil) }
end

function gen_func_558(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 558 + B
end

function gen_func_559(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 559
  end
  return sum + (a or 0)
end

function gen_func_560(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 560, val = A + 0, ok = (b ~= nil) }
end

function gen_func_561(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 561 + B
end

function gen_func_562(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 562
  end
  return sum + (a or 0)
end

function gen_func_563(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 563, val = A + 3, ok = (b ~= nil) }
end

function gen_func_564(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 564 + B
end

function gen_func_565(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 565
  end
  return sum + (a or 0)
end

function gen_func_566(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 566, val = A + 6, ok = (b ~= nil) }
end

function gen_func_567(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 567 + B
end

function gen_func_568(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 568
  end
  return sum + (a or 0)
end

function gen_func_569(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 569, val = A + 9, ok = (b ~= nil) }
end

function gen_func_570(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 570 + B
end

function gen_func_571(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 571
  end
  return sum + (a or 0)
end

function gen_func_572(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 572, val = A + 2, ok = (b ~= nil) }
end

function gen_func_573(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 573 + B
end

function gen_func_574(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 574
  end
  return sum + (a or 0)
end

function gen_func_575(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 575, val = A + 5, ok = (b ~= nil) }
end

function gen_func_576(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 576 + B
end

function gen_func_577(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 577
  end
  return sum + (a or 0)
end

function gen_func_578(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 578, val = A + 8, ok = (b ~= nil) }
end

function gen_func_579(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 579 + B
end

function gen_func_580(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 580
  end
  return sum + (a or 0)
end

function gen_func_581(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 581, val = A + 1, ok = (b ~= nil) }
end

function gen_func_582(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 582 + B
end

function gen_func_583(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 583
  end
  return sum + (a or 0)
end

function gen_func_584(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 584, val = A + 4, ok = (b ~= nil) }
end

function gen_func_585(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 585 + B
end

function gen_func_586(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 586
  end
  return sum + (a or 0)
end

function gen_func_587(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 587, val = A + 7, ok = (b ~= nil) }
end

function gen_func_588(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 588 + B
end

function gen_func_589(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 589
  end
  return sum + (a or 0)
end

function gen_func_590(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 590, val = A + 0, ok = (b ~= nil) }
end

function gen_func_591(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 591 + B
end

function gen_func_592(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 592
  end
  return sum + (a or 0)
end

function gen_func_593(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 593, val = A + 3, ok = (b ~= nil) }
end

function gen_func_594(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 594 + B
end

function gen_func_595(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 595
  end
  return sum + (a or 0)
end

function gen_func_596(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 596, val = A + 6, ok = (b ~= nil) }
end

function gen_func_597(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 597 + B
end

function gen_func_598(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 598
  end
  return sum + (a or 0)
end

function gen_func_599(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 599, val = A + 9, ok = (b ~= nil) }
end

function gen_func_600(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 600 + B
end

function gen_func_601(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 601
  end
  return sum + (a or 0)
end

function gen_func_602(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 602, val = A + 2, ok = (b ~= nil) }
end

function gen_func_603(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 603 + B
end

function gen_func_604(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 604
  end
  return sum + (a or 0)
end

function gen_func_605(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 605, val = A + 5, ok = (b ~= nil) }
end

function gen_func_606(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 606 + B
end

function gen_func_607(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 607
  end
  return sum + (a or 0)
end

function gen_func_608(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 608, val = A + 8, ok = (b ~= nil) }
end

function gen_func_609(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 609 + B
end

function gen_func_610(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 610
  end
  return sum + (a or 0)
end

function gen_func_611(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 611, val = A + 1, ok = (b ~= nil) }
end

function gen_func_612(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 612 + B
end

function gen_func_613(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 613
  end
  return sum + (a or 0)
end

function gen_func_614(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 614, val = A + 4, ok = (b ~= nil) }
end

function gen_func_615(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 615 + B
end

function gen_func_616(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 616
  end
  return sum + (a or 0)
end

function gen_func_617(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 617, val = A + 7, ok = (b ~= nil) }
end

function gen_func_618(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 618 + B
end

function gen_func_619(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 619
  end
  return sum + (a or 0)
end

function gen_func_620(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 620, val = A + 0, ok = (b ~= nil) }
end

function gen_func_621(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 621 + B
end

function gen_func_622(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 622
  end
  return sum + (a or 0)
end

function gen_func_623(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 623, val = A + 3, ok = (b ~= nil) }
end

function gen_func_624(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 624 + B
end

function gen_func_625(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 625
  end
  return sum + (a or 0)
end

function gen_func_626(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 626, val = A + 6, ok = (b ~= nil) }
end

function gen_func_627(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 627 + B
end

function gen_func_628(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 628
  end
  return sum + (a or 0)
end

function gen_func_629(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 629, val = A + 9, ok = (b ~= nil) }
end

function gen_func_630(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 630 + B
end

function gen_func_631(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 631
  end
  return sum + (a or 0)
end

function gen_func_632(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 632, val = A + 2, ok = (b ~= nil) }
end

function gen_func_633(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 633 + B
end

function gen_func_634(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 634
  end
  return sum + (a or 0)
end

function gen_func_635(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 635, val = A + 5, ok = (b ~= nil) }
end

function gen_func_636(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 636 + B
end

function gen_func_637(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 637
  end
  return sum + (a or 0)
end

function gen_func_638(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 638, val = A + 8, ok = (b ~= nil) }
end

function gen_func_639(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 639 + B
end

function gen_func_640(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 640
  end
  return sum + (a or 0)
end

function gen_func_641(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 641, val = A + 1, ok = (b ~= nil) }
end

function gen_func_642(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 642 + B
end

function gen_func_643(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 643
  end
  return sum + (a or 0)
end

function gen_func_644(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 644, val = A + 4, ok = (b ~= nil) }
end

function gen_func_645(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 645 + B
end

function gen_func_646(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 646
  end
  return sum + (a or 0)
end

function gen_func_647(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 647, val = A + 7, ok = (b ~= nil) }
end

function gen_func_648(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 648 + B
end

function gen_func_649(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 649
  end
  return sum + (a or 0)
end

function gen_func_650(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 650, val = A + 0, ok = (b ~= nil) }
end

function gen_func_651(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 651 + B
end

function gen_func_652(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 652
  end
  return sum + (a or 0)
end

function gen_func_653(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 653, val = A + 3, ok = (b ~= nil) }
end

function gen_func_654(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 654 + B
end

function gen_func_655(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 655
  end
  return sum + (a or 0)
end

function gen_func_656(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 656, val = A + 6, ok = (b ~= nil) }
end

function gen_func_657(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 657 + B
end

function gen_func_658(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 658
  end
  return sum + (a or 0)
end

function gen_func_659(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 659, val = A + 9, ok = (b ~= nil) }
end

function gen_func_660(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 660 + B
end

function gen_func_661(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 661
  end
  return sum + (a or 0)
end

function gen_func_662(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 662, val = A + 2, ok = (b ~= nil) }
end

function gen_func_663(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 663 + B
end

function gen_func_664(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 664
  end
  return sum + (a or 0)
end

function gen_func_665(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 665, val = A + 5, ok = (b ~= nil) }
end

function gen_func_666(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 666 + B
end

function gen_func_667(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 667
  end
  return sum + (a or 0)
end

function gen_func_668(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 668, val = A + 8, ok = (b ~= nil) }
end

function gen_func_669(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 669 + B
end

function gen_func_670(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 670
  end
  return sum + (a or 0)
end

function gen_func_671(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 671, val = A + 1, ok = (b ~= nil) }
end

function gen_func_672(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 672 + B
end

function gen_func_673(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 673
  end
  return sum + (a or 0)
end

function gen_func_674(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 674, val = A + 4, ok = (b ~= nil) }
end

function gen_func_675(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 675 + B
end

function gen_func_676(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 676
  end
  return sum + (a or 0)
end

function gen_func_677(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 677, val = A + 7, ok = (b ~= nil) }
end

function gen_func_678(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 678 + B
end

function gen_func_679(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 679
  end
  return sum + (a or 0)
end

function gen_func_680(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 680, val = A + 0, ok = (b ~= nil) }
end

function gen_func_681(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 681 + B
end

function gen_func_682(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 682
  end
  return sum + (a or 0)
end

function gen_func_683(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 683, val = A + 3, ok = (b ~= nil) }
end

function gen_func_684(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 684 + B
end

function gen_func_685(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 685
  end
  return sum + (a or 0)
end

function gen_func_686(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 686, val = A + 6, ok = (b ~= nil) }
end

function gen_func_687(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 687 + B
end

function gen_func_688(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 688
  end
  return sum + (a or 0)
end

function gen_func_689(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 689, val = A + 9, ok = (b ~= nil) }
end

function gen_func_690(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 690 + B
end

function gen_func_691(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 691
  end
  return sum + (a or 0)
end

function gen_func_692(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 692, val = A + 2, ok = (b ~= nil) }
end

function gen_func_693(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 693 + B
end

function gen_func_694(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 694
  end
  return sum + (a or 0)
end

function gen_func_695(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 695, val = A + 5, ok = (b ~= nil) }
end

function gen_func_696(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 696 + B
end

function gen_func_697(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 697
  end
  return sum + (a or 0)
end

function gen_func_698(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 698, val = A + 8, ok = (b ~= nil) }
end

function gen_func_699(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 699 + B
end

function gen_func_700(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 700
  end
  return sum + (a or 0)
end

function gen_func_701(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 701, val = A + 1, ok = (b ~= nil) }
end

function gen_func_702(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 702 + B
end

function gen_func_703(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 703
  end
  return sum + (a or 0)
end

function gen_func_704(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 704, val = A + 4, ok = (b ~= nil) }
end

function gen_func_705(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 705 + B
end

function gen_func_706(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 706
  end
  return sum + (a or 0)
end

function gen_func_707(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 707, val = A + 7, ok = (b ~= nil) }
end

function gen_func_708(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 708 + B
end

function gen_func_709(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 709
  end
  return sum + (a or 0)
end

function gen_func_710(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 710, val = A + 0, ok = (b ~= nil) }
end

function gen_func_711(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 711 + B
end

function gen_func_712(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 712
  end
  return sum + (a or 0)
end

function gen_func_713(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 713, val = A + 3, ok = (b ~= nil) }
end

function gen_func_714(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 714 + B
end

function gen_func_715(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 715
  end
  return sum + (a or 0)
end

function gen_func_716(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 716, val = A + 6, ok = (b ~= nil) }
end

function gen_func_717(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 717 + B
end

function gen_func_718(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 718
  end
  return sum + (a or 0)
end

function gen_func_719(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 719, val = A + 9, ok = (b ~= nil) }
end

function gen_func_720(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 720 + B
end

function gen_func_721(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 721
  end
  return sum + (a or 0)
end

function gen_func_722(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 722, val = A + 2, ok = (b ~= nil) }
end

function gen_func_723(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 723 + B
end

function gen_func_724(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 724
  end
  return sum + (a or 0)
end

function gen_func_725(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 725, val = A + 5, ok = (b ~= nil) }
end

function gen_func_726(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 726 + B
end

function gen_func_727(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 727
  end
  return sum + (a or 0)
end

function gen_func_728(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 728, val = A + 8, ok = (b ~= nil) }
end

function gen_func_729(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 729 + B
end

function gen_func_730(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 730
  end
  return sum + (a or 0)
end

function gen_func_731(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 731, val = A + 1, ok = (b ~= nil) }
end

function gen_func_732(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 732 + B
end

function gen_func_733(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 733
  end
  return sum + (a or 0)
end

function gen_func_734(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 734, val = A + 4, ok = (b ~= nil) }
end

function gen_func_735(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 735 + B
end

function gen_func_736(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 736
  end
  return sum + (a or 0)
end

function gen_func_737(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 737, val = A + 7, ok = (b ~= nil) }
end

function gen_func_738(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 738 + B
end

function gen_func_739(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 739
  end
  return sum + (a or 0)
end

function gen_func_740(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 740, val = A + 0, ok = (b ~= nil) }
end

function gen_func_741(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 741 + B
end

function gen_func_742(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 742
  end
  return sum + (a or 0)
end

function gen_func_743(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 743, val = A + 3, ok = (b ~= nil) }
end

function gen_func_744(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 744 + B
end

function gen_func_745(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 745
  end
  return sum + (a or 0)
end

function gen_func_746(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 746, val = A + 6, ok = (b ~= nil) }
end

function gen_func_747(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 747 + B
end

function gen_func_748(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 748
  end
  return sum + (a or 0)
end

function gen_func_749(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 749, val = A + 9, ok = (b ~= nil) }
end

function gen_func_750(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 750 + B
end

function gen_func_751(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 751
  end
  return sum + (a or 0)
end

function gen_func_752(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 752, val = A + 2, ok = (b ~= nil) }
end

function gen_func_753(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 753 + B
end

function gen_func_754(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 754
  end
  return sum + (a or 0)
end

function gen_func_755(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 755, val = A + 5, ok = (b ~= nil) }
end

function gen_func_756(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 756 + B
end

function gen_func_757(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 757
  end
  return sum + (a or 0)
end

function gen_func_758(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 758, val = A + 8, ok = (b ~= nil) }
end

function gen_func_759(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 759 + B
end

function gen_func_760(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 760
  end
  return sum + (a or 0)
end

function gen_func_761(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 761, val = A + 1, ok = (b ~= nil) }
end

function gen_func_762(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 762 + B
end

function gen_func_763(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 763
  end
  return sum + (a or 0)
end

function gen_func_764(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 764, val = A + 4, ok = (b ~= nil) }
end

function gen_func_765(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 765 + B
end

function gen_func_766(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 766
  end
  return sum + (a or 0)
end

function gen_func_767(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 767, val = A + 7, ok = (b ~= nil) }
end

function gen_func_768(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 768 + B
end

function gen_func_769(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 769
  end
  return sum + (a or 0)
end

function gen_func_770(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 770, val = A + 0, ok = (b ~= nil) }
end

function gen_func_771(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 771 + B
end

function gen_func_772(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 772
  end
  return sum + (a or 0)
end

function gen_func_773(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 773, val = A + 3, ok = (b ~= nil) }
end

function gen_func_774(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 774 + B
end

function gen_func_775(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 775
  end
  return sum + (a or 0)
end

function gen_func_776(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 776, val = A + 6, ok = (b ~= nil) }
end

function gen_func_777(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 777 + B
end

function gen_func_778(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 778
  end
  return sum + (a or 0)
end

function gen_func_779(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 779, val = A + 9, ok = (b ~= nil) }
end

function gen_func_780(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 780 + B
end

function gen_func_781(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 781
  end
  return sum + (a or 0)
end

function gen_func_782(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 782, val = A + 2, ok = (b ~= nil) }
end

function gen_func_783(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 783 + B
end

function gen_func_784(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 784
  end
  return sum + (a or 0)
end

function gen_func_785(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 785, val = A + 5, ok = (b ~= nil) }
end

function gen_func_786(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 786 + B
end

function gen_func_787(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 787
  end
  return sum + (a or 0)
end

function gen_func_788(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 788, val = A + 8, ok = (b ~= nil) }
end

function gen_func_789(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 789 + B
end

function gen_func_790(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 790
  end
  return sum + (a or 0)
end

function gen_func_791(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 791, val = A + 1, ok = (b ~= nil) }
end

function gen_func_792(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 792 + B
end

function gen_func_793(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 793
  end
  return sum + (a or 0)
end

function gen_func_794(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 794, val = A + 4, ok = (b ~= nil) }
end

function gen_func_795(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 795 + B
end

function gen_func_796(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 796
  end
  return sum + (a or 0)
end

function gen_func_797(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 797, val = A + 7, ok = (b ~= nil) }
end

function gen_func_798(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 798 + B
end

function gen_func_799(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 799
  end
  return sum + (a or 0)
end

function gen_func_800(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 800, val = A + 0, ok = (b ~= nil) }
end

function gen_func_801(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 801 + B
end

function gen_func_802(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 802
  end
  return sum + (a or 0)
end

function gen_func_803(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 803, val = A + 3, ok = (b ~= nil) }
end

function gen_func_804(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 804 + B
end

function gen_func_805(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 805
  end
  return sum + (a or 0)
end

function gen_func_806(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 806, val = A + 6, ok = (b ~= nil) }
end

function gen_func_807(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 807 + B
end

function gen_func_808(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 808
  end
  return sum + (a or 0)
end

function gen_func_809(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 809, val = A + 9, ok = (b ~= nil) }
end

function gen_func_810(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 810 + B
end

function gen_func_811(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 811
  end
  return sum + (a or 0)
end

function gen_func_812(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 812, val = A + 2, ok = (b ~= nil) }
end

function gen_func_813(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 813 + B
end

function gen_func_814(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 814
  end
  return sum + (a or 0)
end

function gen_func_815(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 815, val = A + 5, ok = (b ~= nil) }
end

function gen_func_816(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 816 + B
end

function gen_func_817(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 817
  end
  return sum + (a or 0)
end

function gen_func_818(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 818, val = A + 8, ok = (b ~= nil) }
end

function gen_func_819(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 819 + B
end

function gen_func_820(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 820
  end
  return sum + (a or 0)
end

function gen_func_821(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 821, val = A + 1, ok = (b ~= nil) }
end

function gen_func_822(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 822 + B
end

function gen_func_823(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 823
  end
  return sum + (a or 0)
end

function gen_func_824(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 824, val = A + 4, ok = (b ~= nil) }
end

function gen_func_825(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 825 + B
end

function gen_func_826(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 826
  end
  return sum + (a or 0)
end

function gen_func_827(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 827, val = A + 7, ok = (b ~= nil) }
end

function gen_func_828(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 828 + B
end

function gen_func_829(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 829
  end
  return sum + (a or 0)
end

function gen_func_830(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 830, val = A + 0, ok = (b ~= nil) }
end

function gen_func_831(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 831 + B
end

function gen_func_832(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 832
  end
  return sum + (a or 0)
end

function gen_func_833(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 833, val = A + 3, ok = (b ~= nil) }
end

function gen_func_834(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 834 + B
end

function gen_func_835(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 835
  end
  return sum + (a or 0)
end

function gen_func_836(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 836, val = A + 6, ok = (b ~= nil) }
end

function gen_func_837(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 837 + B
end

function gen_func_838(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 838
  end
  return sum + (a or 0)
end

function gen_func_839(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 839, val = A + 9, ok = (b ~= nil) }
end

function gen_func_840(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 840 + B
end

function gen_func_841(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 841
  end
  return sum + (a or 0)
end

function gen_func_842(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 842, val = A + 2, ok = (b ~= nil) }
end

function gen_func_843(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 843 + B
end

function gen_func_844(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 844
  end
  return sum + (a or 0)
end

function gen_func_845(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 845, val = A + 5, ok = (b ~= nil) }
end

function gen_func_846(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 846 + B
end

function gen_func_847(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 847
  end
  return sum + (a or 0)
end

function gen_func_848(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 848, val = A + 8, ok = (b ~= nil) }
end

function gen_func_849(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 849 + B
end

function gen_func_850(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 850
  end
  return sum + (a or 0)
end

function gen_func_851(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 851, val = A + 1, ok = (b ~= nil) }
end

function gen_func_852(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 852 + B
end

function gen_func_853(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 853
  end
  return sum + (a or 0)
end

function gen_func_854(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 854, val = A + 4, ok = (b ~= nil) }
end

function gen_func_855(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 855 + B
end

function gen_func_856(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 856
  end
  return sum + (a or 0)
end

function gen_func_857(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 857, val = A + 7, ok = (b ~= nil) }
end

function gen_func_858(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 858 + B
end

function gen_func_859(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 859
  end
  return sum + (a or 0)
end

function gen_func_860(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 860, val = A + 0, ok = (b ~= nil) }
end

function gen_func_861(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 861 + B
end

function gen_func_862(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 862
  end
  return sum + (a or 0)
end

function gen_func_863(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 863, val = A + 3, ok = (b ~= nil) }
end

function gen_func_864(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 864 + B
end

function gen_func_865(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 865
  end
  return sum + (a or 0)
end

function gen_func_866(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 866, val = A + 6, ok = (b ~= nil) }
end

function gen_func_867(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 867 + B
end

function gen_func_868(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 868
  end
  return sum + (a or 0)
end

function gen_func_869(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 869, val = A + 9, ok = (b ~= nil) }
end

function gen_func_870(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 870 + B
end

function gen_func_871(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 871
  end
  return sum + (a or 0)
end

function gen_func_872(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 872, val = A + 2, ok = (b ~= nil) }
end

function gen_func_873(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 873 + B
end

function gen_func_874(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 874
  end
  return sum + (a or 0)
end

function gen_func_875(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 875, val = A + 5, ok = (b ~= nil) }
end

function gen_func_876(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 876 + B
end

function gen_func_877(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 877
  end
  return sum + (a or 0)
end

function gen_func_878(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 878, val = A + 8, ok = (b ~= nil) }
end

function gen_func_879(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 879 + B
end

function gen_func_880(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 880
  end
  return sum + (a or 0)
end

function gen_func_881(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 881, val = A + 1, ok = (b ~= nil) }
end

function gen_func_882(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 882 + B
end

function gen_func_883(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 883
  end
  return sum + (a or 0)
end

function gen_func_884(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 884, val = A + 4, ok = (b ~= nil) }
end

function gen_func_885(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 885 + B
end

function gen_func_886(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 886
  end
  return sum + (a or 0)
end

function gen_func_887(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 887, val = A + 7, ok = (b ~= nil) }
end

function gen_func_888(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 888 + B
end

function gen_func_889(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 889
  end
  return sum + (a or 0)
end

function gen_func_890(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 890, val = A + 0, ok = (b ~= nil) }
end

function gen_func_891(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 891 + B
end

function gen_func_892(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 892
  end
  return sum + (a or 0)
end

function gen_func_893(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 893, val = A + 3, ok = (b ~= nil) }
end

function gen_func_894(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 894 + B
end

function gen_func_895(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 895
  end
  return sum + (a or 0)
end

function gen_func_896(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 896, val = A + 6, ok = (b ~= nil) }
end

function gen_func_897(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 897 + B
end

function gen_func_898(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 898
  end
  return sum + (a or 0)
end

function gen_func_899(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 899, val = A + 9, ok = (b ~= nil) }
end

function gen_func_900(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 900 + B
end

function gen_func_901(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 901
  end
  return sum + (a or 0)
end

function gen_func_902(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 902, val = A + 2, ok = (b ~= nil) }
end

function gen_func_903(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 903 + B
end

function gen_func_904(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 904
  end
  return sum + (a or 0)
end

function gen_func_905(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 905, val = A + 5, ok = (b ~= nil) }
end

function gen_func_906(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 906 + B
end

function gen_func_907(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 907
  end
  return sum + (a or 0)
end

function gen_func_908(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 908, val = A + 8, ok = (b ~= nil) }
end

function gen_func_909(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 909 + B
end

function gen_func_910(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 910
  end
  return sum + (a or 0)
end

function gen_func_911(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 911, val = A + 1, ok = (b ~= nil) }
end

function gen_func_912(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 912 + B
end

function gen_func_913(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 913
  end
  return sum + (a or 0)
end

function gen_func_914(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 914, val = A + 4, ok = (b ~= nil) }
end

function gen_func_915(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 915 + B
end

function gen_func_916(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 916
  end
  return sum + (a or 0)
end

function gen_func_917(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 917, val = A + 7, ok = (b ~= nil) }
end

function gen_func_918(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 918 + B
end

function gen_func_919(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 919
  end
  return sum + (a or 0)
end

function gen_func_920(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 920, val = A + 0, ok = (b ~= nil) }
end

function gen_func_921(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 921 + B
end

function gen_func_922(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 922
  end
  return sum + (a or 0)
end

function gen_func_923(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 923, val = A + 3, ok = (b ~= nil) }
end

function gen_func_924(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 924 + B
end

function gen_func_925(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 925
  end
  return sum + (a or 0)
end

function gen_func_926(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 926, val = A + 6, ok = (b ~= nil) }
end

function gen_func_927(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 927 + B
end

function gen_func_928(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 928
  end
  return sum + (a or 0)
end

function gen_func_929(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 929, val = A + 9, ok = (b ~= nil) }
end

function gen_func_930(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 930 + B
end

function gen_func_931(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 931
  end
  return sum + (a or 0)
end

function gen_func_932(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 932, val = A + 2, ok = (b ~= nil) }
end

function gen_func_933(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 933 + B
end

function gen_func_934(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 934
  end
  return sum + (a or 0)
end

function gen_func_935(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 935, val = A + 5, ok = (b ~= nil) }
end

function gen_func_936(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 936 + B
end

function gen_func_937(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 937
  end
  return sum + (a or 0)
end

function gen_func_938(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 938, val = A + 8, ok = (b ~= nil) }
end

function gen_func_939(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 939 + B
end

function gen_func_940(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 940
  end
  return sum + (a or 0)
end

function gen_func_941(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 941, val = A + 1, ok = (b ~= nil) }
end

function gen_func_942(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 942 + B
end

function gen_func_943(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 943
  end
  return sum + (a or 0)
end

function gen_func_944(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 944, val = A + 4, ok = (b ~= nil) }
end

function gen_func_945(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 945 + B
end

function gen_func_946(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 946
  end
  return sum + (a or 0)
end

function gen_func_947(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 947, val = A + 7, ok = (b ~= nil) }
end

function gen_func_948(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 948 + B
end

function gen_func_949(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 949
  end
  return sum + (a or 0)
end

function gen_func_950(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 950, val = A + 0, ok = (b ~= nil) }
end

function gen_func_951(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 951 + B
end

function gen_func_952(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 952
  end
  return sum + (a or 0)
end

function gen_func_953(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 953, val = A + 3, ok = (b ~= nil) }
end

function gen_func_954(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 954 + B
end

function gen_func_955(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 955
  end
  return sum + (a or 0)
end

function gen_func_956(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 956, val = A + 6, ok = (b ~= nil) }
end

function gen_func_957(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 957 + B
end

function gen_func_958(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 958
  end
  return sum + (a or 0)
end

function gen_func_959(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 959, val = A + 9, ok = (b ~= nil) }
end

function gen_func_960(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 960 + B
end

function gen_func_961(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 961
  end
  return sum + (a or 0)
end

function gen_func_962(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 962, val = A + 2, ok = (b ~= nil) }
end

function gen_func_963(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 963 + B
end

function gen_func_964(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 964
  end
  return sum + (a or 0)
end

function gen_func_965(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 965, val = A + 5, ok = (b ~= nil) }
end

function gen_func_966(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 966 + B
end

function gen_func_967(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 967
  end
  return sum + (a or 0)
end

function gen_func_968(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 968, val = A + 8, ok = (b ~= nil) }
end

function gen_func_969(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 969 + B
end

function gen_func_970(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 970
  end
  return sum + (a or 0)
end

function gen_func_971(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 971, val = A + 1, ok = (b ~= nil) }
end

function gen_func_972(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 972 + B
end

function gen_func_973(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 973
  end
  return sum + (a or 0)
end

function gen_func_974(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 974, val = A + 4, ok = (b ~= nil) }
end

function gen_func_975(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 975 + B
end

function gen_func_976(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 976
  end
  return sum + (a or 0)
end

function gen_func_977(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 977, val = A + 7, ok = (b ~= nil) }
end

function gen_func_978(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 978 + B
end

function gen_func_979(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 979
  end
  return sum + (a or 0)
end

function gen_func_980(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 980, val = A + 0, ok = (b ~= nil) }
end

function gen_func_981(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 981 + B
end

function gen_func_982(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 982
  end
  return sum + (a or 0)
end

function gen_func_983(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 983, val = A + 3, ok = (b ~= nil) }
end

function gen_func_984(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 984 + B
end

function gen_func_985(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 985
  end
  return sum + (a or 0)
end

function gen_func_986(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 986, val = A + 6, ok = (b ~= nil) }
end

function gen_func_987(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 987 + B
end

function gen_func_988(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 988
  end
  return sum + (a or 0)
end

function gen_func_989(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 989, val = A + 9, ok = (b ~= nil) }
end

function gen_func_990(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 990 + B
end

function gen_func_991(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 991
  end
  return sum + (a or 0)
end

function gen_func_992(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 992, val = A + 2, ok = (b ~= nil) }
end

function gen_func_993(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 993 + B
end

function gen_func_994(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 994
  end
  return sum + (a or 0)
end

function gen_func_995(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 995, val = A + 5, ok = (b ~= nil) }
end

function gen_func_996(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 996 + B
end

function gen_func_997(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 997
  end
  return sum + (a or 0)
end

function gen_func_998(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 998, val = A + 8, ok = (b ~= nil) }
end

function gen_func_999(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 999 + B
end

function gen_func_1000(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1000
  end
  return sum + (a or 0)
end

function gen_func_1001(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1001, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1002(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1002 + B
end

function gen_func_1003(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1003
  end
  return sum + (a or 0)
end

function gen_func_1004(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1004, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1005(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1005 + B
end

function gen_func_1006(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1006
  end
  return sum + (a or 0)
end

function gen_func_1007(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1007, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1008(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1008 + B
end

function gen_func_1009(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1009
  end
  return sum + (a or 0)
end

function gen_func_1010(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1010, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1011(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1011 + B
end

function gen_func_1012(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1012
  end
  return sum + (a or 0)
end

function gen_func_1013(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1013, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1014(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1014 + B
end

function gen_func_1015(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1015
  end
  return sum + (a or 0)
end

function gen_func_1016(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1016, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1017(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1017 + B
end

function gen_func_1018(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1018
  end
  return sum + (a or 0)
end

function gen_func_1019(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1019, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1020(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1020 + B
end

function gen_func_1021(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1021
  end
  return sum + (a or 0)
end

function gen_func_1022(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1022, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1023(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1023 + B
end

function gen_func_1024(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1024
  end
  return sum + (a or 0)
end

function gen_func_1025(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1025, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1026(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1026 + B
end

function gen_func_1027(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1027
  end
  return sum + (a or 0)
end

function gen_func_1028(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1028, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1029(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1029 + B
end

function gen_func_1030(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1030
  end
  return sum + (a or 0)
end

function gen_func_1031(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1031, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1032(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1032 + B
end

function gen_func_1033(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1033
  end
  return sum + (a or 0)
end

function gen_func_1034(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1034, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1035(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1035 + B
end

function gen_func_1036(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1036
  end
  return sum + (a or 0)
end

function gen_func_1037(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1037, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1038(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1038 + B
end

function gen_func_1039(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1039
  end
  return sum + (a or 0)
end

function gen_func_1040(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1040, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1041(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1041 + B
end

function gen_func_1042(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1042
  end
  return sum + (a or 0)
end

function gen_func_1043(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1043, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1044(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1044 + B
end

function gen_func_1045(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1045
  end
  return sum + (a or 0)
end

function gen_func_1046(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1046, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1047(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1047 + B
end

function gen_func_1048(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1048
  end
  return sum + (a or 0)
end

function gen_func_1049(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1049, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1050(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1050 + B
end

function gen_func_1051(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1051
  end
  return sum + (a or 0)
end

function gen_func_1052(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1052, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1053(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1053 + B
end

function gen_func_1054(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1054
  end
  return sum + (a or 0)
end

function gen_func_1055(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1055, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1056(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1056 + B
end

function gen_func_1057(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1057
  end
  return sum + (a or 0)
end

function gen_func_1058(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1058, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1059(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1059 + B
end

function gen_func_1060(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1060
  end
  return sum + (a or 0)
end

function gen_func_1061(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1061, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1062(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1062 + B
end

function gen_func_1063(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1063
  end
  return sum + (a or 0)
end

function gen_func_1064(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1064, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1065(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1065 + B
end

function gen_func_1066(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1066
  end
  return sum + (a or 0)
end

function gen_func_1067(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1067, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1068(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1068 + B
end

function gen_func_1069(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1069
  end
  return sum + (a or 0)
end

function gen_func_1070(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1070, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1071(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1071 + B
end

function gen_func_1072(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1072
  end
  return sum + (a or 0)
end

function gen_func_1073(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1073, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1074(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1074 + B
end

function gen_func_1075(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1075
  end
  return sum + (a or 0)
end

function gen_func_1076(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1076, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1077(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1077 + B
end

function gen_func_1078(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1078
  end
  return sum + (a or 0)
end

function gen_func_1079(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1079, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1080(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1080 + B
end

function gen_func_1081(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1081
  end
  return sum + (a or 0)
end

function gen_func_1082(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1082, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1083(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1083 + B
end

function gen_func_1084(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1084
  end
  return sum + (a or 0)
end

function gen_func_1085(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1085, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1086(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1086 + B
end

function gen_func_1087(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1087
  end
  return sum + (a or 0)
end

function gen_func_1088(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1088, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1089(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1089 + B
end

function gen_func_1090(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1090
  end
  return sum + (a or 0)
end

function gen_func_1091(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1091, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1092(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1092 + B
end

function gen_func_1093(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1093
  end
  return sum + (a or 0)
end

function gen_func_1094(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1094, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1095(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1095 + B
end

function gen_func_1096(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1096
  end
  return sum + (a or 0)
end

function gen_func_1097(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1097, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1098(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1098 + B
end

function gen_func_1099(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1099
  end
  return sum + (a or 0)
end

function gen_func_1100(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1100, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1101(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1101 + B
end

function gen_func_1102(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1102
  end
  return sum + (a or 0)
end

function gen_func_1103(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1103, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1104(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1104 + B
end

function gen_func_1105(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1105
  end
  return sum + (a or 0)
end

function gen_func_1106(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1106, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1107(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1107 + B
end

function gen_func_1108(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1108
  end
  return sum + (a or 0)
end

function gen_func_1109(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1109, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1110(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1110 + B
end

function gen_func_1111(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1111
  end
  return sum + (a or 0)
end

function gen_func_1112(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1112, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1113(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1113 + B
end

function gen_func_1114(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1114
  end
  return sum + (a or 0)
end

function gen_func_1115(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1115, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1116(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1116 + B
end

function gen_func_1117(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1117
  end
  return sum + (a or 0)
end

function gen_func_1118(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1118, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1119(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1119 + B
end

function gen_func_1120(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1120
  end
  return sum + (a or 0)
end

function gen_func_1121(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1121, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1122(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1122 + B
end

function gen_func_1123(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1123
  end
  return sum + (a or 0)
end

function gen_func_1124(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1124, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1125(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1125 + B
end

function gen_func_1126(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1126
  end
  return sum + (a or 0)
end

function gen_func_1127(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1127, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1128(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1128 + B
end

function gen_func_1129(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1129
  end
  return sum + (a or 0)
end

function gen_func_1130(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1130, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1131(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1131 + B
end

function gen_func_1132(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1132
  end
  return sum + (a or 0)
end

function gen_func_1133(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1133, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1134(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1134 + B
end

function gen_func_1135(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1135
  end
  return sum + (a or 0)
end

function gen_func_1136(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1136, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1137(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1137 + B
end

function gen_func_1138(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1138
  end
  return sum + (a or 0)
end

function gen_func_1139(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1139, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1140(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1140 + B
end

function gen_func_1141(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1141
  end
  return sum + (a or 0)
end

function gen_func_1142(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1142, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1143(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1143 + B
end

function gen_func_1144(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1144
  end
  return sum + (a or 0)
end

function gen_func_1145(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1145, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1146(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1146 + B
end

function gen_func_1147(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1147
  end
  return sum + (a or 0)
end

function gen_func_1148(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1148, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1149(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1149 + B
end

function gen_func_1150(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1150
  end
  return sum + (a or 0)
end

function gen_func_1151(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1151, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1152(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1152 + B
end

function gen_func_1153(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1153
  end
  return sum + (a or 0)
end

function gen_func_1154(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1154, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1155(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1155 + B
end

function gen_func_1156(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1156
  end
  return sum + (a or 0)
end

function gen_func_1157(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1157, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1158(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1158 + B
end

function gen_func_1159(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1159
  end
  return sum + (a or 0)
end

function gen_func_1160(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1160, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1161(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1161 + B
end

function gen_func_1162(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1162
  end
  return sum + (a or 0)
end

function gen_func_1163(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1163, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1164(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1164 + B
end

function gen_func_1165(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1165
  end
  return sum + (a or 0)
end

function gen_func_1166(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1166, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1167(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1167 + B
end

function gen_func_1168(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1168
  end
  return sum + (a or 0)
end

function gen_func_1169(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1169, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1170(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1170 + B
end

function gen_func_1171(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1171
  end
  return sum + (a or 0)
end

function gen_func_1172(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1172, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1173(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1173 + B
end

function gen_func_1174(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1174
  end
  return sum + (a or 0)
end

function gen_func_1175(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1175, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1176(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1176 + B
end

function gen_func_1177(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1177
  end
  return sum + (a or 0)
end

function gen_func_1178(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1178, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1179(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1179 + B
end

function gen_func_1180(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1180
  end
  return sum + (a or 0)
end

function gen_func_1181(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1181, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1182(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1182 + B
end

function gen_func_1183(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1183
  end
  return sum + (a or 0)
end

function gen_func_1184(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1184, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1185(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1185 + B
end

function gen_func_1186(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1186
  end
  return sum + (a or 0)
end

function gen_func_1187(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1187, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1188(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1188 + B
end

function gen_func_1189(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1189
  end
  return sum + (a or 0)
end

function gen_func_1190(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1190, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1191(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1191 + B
end

function gen_func_1192(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1192
  end
  return sum + (a or 0)
end

function gen_func_1193(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1193, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1194(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1194 + B
end

function gen_func_1195(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1195
  end
  return sum + (a or 0)
end

function gen_func_1196(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1196, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1197(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1197 + B
end

function gen_func_1198(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1198
  end
  return sum + (a or 0)
end

function gen_func_1199(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1199, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1200(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1200 + B
end

function gen_func_1201(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1201
  end
  return sum + (a or 0)
end

function gen_func_1202(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1202, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1203(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1203 + B
end

function gen_func_1204(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1204
  end
  return sum + (a or 0)
end

function gen_func_1205(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1205, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1206(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1206 + B
end

function gen_func_1207(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1207
  end
  return sum + (a or 0)
end

function gen_func_1208(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1208, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1209(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1209 + B
end

function gen_func_1210(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1210
  end
  return sum + (a or 0)
end

function gen_func_1211(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1211, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1212(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1212 + B
end

function gen_func_1213(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1213
  end
  return sum + (a or 0)
end

function gen_func_1214(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1214, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1215(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1215 + B
end

function gen_func_1216(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1216
  end
  return sum + (a or 0)
end

function gen_func_1217(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1217, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1218(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1218 + B
end

function gen_func_1219(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1219
  end
  return sum + (a or 0)
end

function gen_func_1220(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1220, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1221(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1221 + B
end

function gen_func_1222(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1222
  end
  return sum + (a or 0)
end

function gen_func_1223(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1223, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1224(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1224 + B
end

function gen_func_1225(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1225
  end
  return sum + (a or 0)
end

function gen_func_1226(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1226, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1227(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1227 + B
end

function gen_func_1228(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1228
  end
  return sum + (a or 0)
end

function gen_func_1229(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1229, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1230(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1230 + B
end

function gen_func_1231(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1231
  end
  return sum + (a or 0)
end

function gen_func_1232(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1232, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1233(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1233 + B
end

function gen_func_1234(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1234
  end
  return sum + (a or 0)
end

function gen_func_1235(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1235, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1236(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1236 + B
end

function gen_func_1237(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1237
  end
  return sum + (a or 0)
end

function gen_func_1238(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1238, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1239(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1239 + B
end

function gen_func_1240(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1240
  end
  return sum + (a or 0)
end

function gen_func_1241(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1241, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1242(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1242 + B
end

function gen_func_1243(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1243
  end
  return sum + (a or 0)
end

function gen_func_1244(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1244, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1245(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1245 + B
end

function gen_func_1246(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1246
  end
  return sum + (a or 0)
end

function gen_func_1247(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1247, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1248(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1248 + B
end

function gen_func_1249(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1249
  end
  return sum + (a or 0)
end

function gen_func_1250(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1250, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1251(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1251 + B
end

function gen_func_1252(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1252
  end
  return sum + (a or 0)
end

function gen_func_1253(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1253, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1254(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1254 + B
end

function gen_func_1255(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1255
  end
  return sum + (a or 0)
end

function gen_func_1256(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1256, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1257(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1257 + B
end

function gen_func_1258(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1258
  end
  return sum + (a or 0)
end

function gen_func_1259(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1259, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1260(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1260 + B
end

function gen_func_1261(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1261
  end
  return sum + (a or 0)
end

function gen_func_1262(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1262, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1263(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1263 + B
end

function gen_func_1264(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1264
  end
  return sum + (a or 0)
end

function gen_func_1265(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1265, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1266(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1266 + B
end

function gen_func_1267(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1267
  end
  return sum + (a or 0)
end

function gen_func_1268(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1268, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1269(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1269 + B
end

function gen_func_1270(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1270
  end
  return sum + (a or 0)
end

function gen_func_1271(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1271, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1272(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1272 + B
end

function gen_func_1273(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1273
  end
  return sum + (a or 0)
end

function gen_func_1274(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1274, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1275(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1275 + B
end

function gen_func_1276(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1276
  end
  return sum + (a or 0)
end

function gen_func_1277(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1277, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1278(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1278 + B
end

function gen_func_1279(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1279
  end
  return sum + (a or 0)
end

function gen_func_1280(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1280, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1281(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1281 + B
end

function gen_func_1282(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1282
  end
  return sum + (a or 0)
end

function gen_func_1283(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1283, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1284(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1284 + B
end

function gen_func_1285(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1285
  end
  return sum + (a or 0)
end

function gen_func_1286(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1286, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1287(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1287 + B
end

function gen_func_1288(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1288
  end
  return sum + (a or 0)
end

function gen_func_1289(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1289, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1290(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1290 + B
end

function gen_func_1291(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1291
  end
  return sum + (a or 0)
end

function gen_func_1292(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1292, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1293(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1293 + B
end

function gen_func_1294(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1294
  end
  return sum + (a or 0)
end

function gen_func_1295(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1295, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1296(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1296 + B
end

function gen_func_1297(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1297
  end
  return sum + (a or 0)
end

function gen_func_1298(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1298, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1299(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1299 + B
end

function gen_func_1300(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1300
  end
  return sum + (a or 0)
end

function gen_func_1301(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1301, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1302(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1302 + B
end

function gen_func_1303(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1303
  end
  return sum + (a or 0)
end

function gen_func_1304(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1304, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1305(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1305 + B
end

function gen_func_1306(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1306
  end
  return sum + (a or 0)
end

function gen_func_1307(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1307, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1308(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1308 + B
end

function gen_func_1309(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1309
  end
  return sum + (a or 0)
end

function gen_func_1310(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1310, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1311(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1311 + B
end

function gen_func_1312(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1312
  end
  return sum + (a or 0)
end

function gen_func_1313(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1313, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1314(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1314 + B
end

function gen_func_1315(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1315
  end
  return sum + (a or 0)
end

function gen_func_1316(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1316, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1317(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1317 + B
end

function gen_func_1318(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1318
  end
  return sum + (a or 0)
end

function gen_func_1319(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1319, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1320(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1320 + B
end

function gen_func_1321(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1321
  end
  return sum + (a or 0)
end

function gen_func_1322(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1322, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1323(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1323 + B
end

function gen_func_1324(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1324
  end
  return sum + (a or 0)
end

function gen_func_1325(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1325, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1326(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1326 + B
end

function gen_func_1327(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1327
  end
  return sum + (a or 0)
end

function gen_func_1328(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1328, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1329(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1329 + B
end

function gen_func_1330(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1330
  end
  return sum + (a or 0)
end

function gen_func_1331(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1331, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1332(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1332 + B
end

function gen_func_1333(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1333
  end
  return sum + (a or 0)
end

function gen_func_1334(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1334, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1335(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1335 + B
end

function gen_func_1336(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1336
  end
  return sum + (a or 0)
end

function gen_func_1337(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1337, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1338(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1338 + B
end

function gen_func_1339(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1339
  end
  return sum + (a or 0)
end

function gen_func_1340(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1340, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1341(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1341 + B
end

function gen_func_1342(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1342
  end
  return sum + (a or 0)
end

function gen_func_1343(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1343, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1344(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1344 + B
end

function gen_func_1345(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1345
  end
  return sum + (a or 0)
end

function gen_func_1346(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1346, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1347(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1347 + B
end

function gen_func_1348(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1348
  end
  return sum + (a or 0)
end

function gen_func_1349(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1349, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1350(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1350 + B
end

function gen_func_1351(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1351
  end
  return sum + (a or 0)
end

function gen_func_1352(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1352, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1353(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1353 + B
end

function gen_func_1354(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1354
  end
  return sum + (a or 0)
end

function gen_func_1355(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1355, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1356(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1356 + B
end

function gen_func_1357(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1357
  end
  return sum + (a or 0)
end

function gen_func_1358(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1358, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1359(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1359 + B
end

function gen_func_1360(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1360
  end
  return sum + (a or 0)
end

function gen_func_1361(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1361, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1362(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1362 + B
end

function gen_func_1363(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1363
  end
  return sum + (a or 0)
end

function gen_func_1364(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1364, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1365(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1365 + B
end

function gen_func_1366(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1366
  end
  return sum + (a or 0)
end

function gen_func_1367(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1367, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1368(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1368 + B
end

function gen_func_1369(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1369
  end
  return sum + (a or 0)
end

function gen_func_1370(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1370, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1371(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1371 + B
end

function gen_func_1372(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1372
  end
  return sum + (a or 0)
end

function gen_func_1373(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1373, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1374(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1374 + B
end

function gen_func_1375(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1375
  end
  return sum + (a or 0)
end

function gen_func_1376(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1376, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1377(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1377 + B
end

function gen_func_1378(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1378
  end
  return sum + (a or 0)
end

function gen_func_1379(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1379, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1380(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1380 + B
end

function gen_func_1381(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1381
  end
  return sum + (a or 0)
end

function gen_func_1382(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1382, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1383(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1383 + B
end

function gen_func_1384(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1384
  end
  return sum + (a or 0)
end

function gen_func_1385(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1385, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1386(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1386 + B
end

function gen_func_1387(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1387
  end
  return sum + (a or 0)
end

function gen_func_1388(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1388, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1389(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1389 + B
end

function gen_func_1390(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1390
  end
  return sum + (a or 0)
end

function gen_func_1391(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1391, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1392(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1392 + B
end

function gen_func_1393(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1393
  end
  return sum + (a or 0)
end

function gen_func_1394(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1394, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1395(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1395 + B
end

function gen_func_1396(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1396
  end
  return sum + (a or 0)
end

function gen_func_1397(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1397, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1398(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1398 + B
end

function gen_func_1399(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1399
  end
  return sum + (a or 0)
end

function gen_func_1400(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1400, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1401(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1401 + B
end

function gen_func_1402(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1402
  end
  return sum + (a or 0)
end

function gen_func_1403(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1403, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1404(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1404 + B
end

function gen_func_1405(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1405
  end
  return sum + (a or 0)
end

function gen_func_1406(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1406, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1407(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1407 + B
end

function gen_func_1408(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1408
  end
  return sum + (a or 0)
end

function gen_func_1409(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1409, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1410(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1410 + B
end

function gen_func_1411(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1411
  end
  return sum + (a or 0)
end

function gen_func_1412(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1412, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1413(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1413 + B
end

function gen_func_1414(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1414
  end
  return sum + (a or 0)
end

function gen_func_1415(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1415, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1416(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1416 + B
end

function gen_func_1417(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1417
  end
  return sum + (a or 0)
end

function gen_func_1418(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1418, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1419(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1419 + B
end

function gen_func_1420(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1420
  end
  return sum + (a or 0)
end

function gen_func_1421(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1421, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1422(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1422 + B
end

function gen_func_1423(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1423
  end
  return sum + (a or 0)
end

function gen_func_1424(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1424, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1425(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1425 + B
end

function gen_func_1426(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1426
  end
  return sum + (a or 0)
end

function gen_func_1427(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1427, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1428(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1428 + B
end

function gen_func_1429(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1429
  end
  return sum + (a or 0)
end

function gen_func_1430(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1430, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1431(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1431 + B
end

function gen_func_1432(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1432
  end
  return sum + (a or 0)
end

function gen_func_1433(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1433, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1434(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1434 + B
end

function gen_func_1435(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1435
  end
  return sum + (a or 0)
end

function gen_func_1436(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1436, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1437(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1437 + B
end

function gen_func_1438(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1438
  end
  return sum + (a or 0)
end

function gen_func_1439(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1439, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1440(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1440 + B
end

function gen_func_1441(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1441
  end
  return sum + (a or 0)
end

function gen_func_1442(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1442, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1443(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1443 + B
end

function gen_func_1444(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1444
  end
  return sum + (a or 0)
end

function gen_func_1445(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1445, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1446(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1446 + B
end

function gen_func_1447(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1447
  end
  return sum + (a or 0)
end

function gen_func_1448(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1448, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1449(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1449 + B
end

function gen_func_1450(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1450
  end
  return sum + (a or 0)
end

function gen_func_1451(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1451, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1452(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1452 + B
end

function gen_func_1453(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1453
  end
  return sum + (a or 0)
end

function gen_func_1454(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1454, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1455(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1455 + B
end

function gen_func_1456(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1456
  end
  return sum + (a or 0)
end

function gen_func_1457(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1457, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1458(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1458 + B
end

function gen_func_1459(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1459
  end
  return sum + (a or 0)
end

function gen_func_1460(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1460, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1461(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1461 + B
end

function gen_func_1462(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1462
  end
  return sum + (a or 0)
end

function gen_func_1463(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1463, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1464(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1464 + B
end

function gen_func_1465(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1465
  end
  return sum + (a or 0)
end

function gen_func_1466(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1466, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1467(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1467 + B
end

function gen_func_1468(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1468
  end
  return sum + (a or 0)
end

function gen_func_1469(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1469, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1470(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1470 + B
end

function gen_func_1471(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1471
  end
  return sum + (a or 0)
end

function gen_func_1472(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1472, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1473(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1473 + B
end

function gen_func_1474(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1474
  end
  return sum + (a or 0)
end

function gen_func_1475(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1475, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1476(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1476 + B
end

function gen_func_1477(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1477
  end
  return sum + (a or 0)
end

function gen_func_1478(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1478, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1479(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1479 + B
end

function gen_func_1480(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1480
  end
  return sum + (a or 0)
end

function gen_func_1481(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1481, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1482(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1482 + B
end

function gen_func_1483(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1483
  end
  return sum + (a or 0)
end

function gen_func_1484(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1484, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1485(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1485 + B
end

function gen_func_1486(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1486
  end
  return sum + (a or 0)
end

function gen_func_1487(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1487, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1488(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1488 + B
end

function gen_func_1489(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1489
  end
  return sum + (a or 0)
end

function gen_func_1490(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1490, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1491(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1491 + B
end

function gen_func_1492(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1492
  end
  return sum + (a or 0)
end

function gen_func_1493(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1493, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1494(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1494 + B
end

function gen_func_1495(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1495
  end
  return sum + (a or 0)
end

function gen_func_1496(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1496, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1497(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1497 + B
end

function gen_func_1498(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1498
  end
  return sum + (a or 0)
end

function gen_func_1499(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1499, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1500(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1500 + B
end

function gen_func_1501(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1501
  end
  return sum + (a or 0)
end

function gen_func_1502(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1502, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1503(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1503 + B
end

function gen_func_1504(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1504
  end
  return sum + (a or 0)
end

function gen_func_1505(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1505, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1506(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1506 + B
end

function gen_func_1507(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1507
  end
  return sum + (a or 0)
end

function gen_func_1508(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1508, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1509(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1509 + B
end

function gen_func_1510(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1510
  end
  return sum + (a or 0)
end

function gen_func_1511(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1511, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1512(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1512 + B
end

function gen_func_1513(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1513
  end
  return sum + (a or 0)
end

function gen_func_1514(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1514, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1515(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1515 + B
end

function gen_func_1516(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1516
  end
  return sum + (a or 0)
end

function gen_func_1517(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1517, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1518(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1518 + B
end

function gen_func_1519(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1519
  end
  return sum + (a or 0)
end

function gen_func_1520(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1520, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1521(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1521 + B
end

function gen_func_1522(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1522
  end
  return sum + (a or 0)
end

function gen_func_1523(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1523, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1524(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1524 + B
end

function gen_func_1525(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1525
  end
  return sum + (a or 0)
end

function gen_func_1526(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1526, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1527(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1527 + B
end

function gen_func_1528(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1528
  end
  return sum + (a or 0)
end

function gen_func_1529(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1529, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1530(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1530 + B
end

function gen_func_1531(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1531
  end
  return sum + (a or 0)
end

function gen_func_1532(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1532, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1533(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1533 + B
end

function gen_func_1534(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1534
  end
  return sum + (a or 0)
end

function gen_func_1535(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1535, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1536(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1536 + B
end

function gen_func_1537(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1537
  end
  return sum + (a or 0)
end

function gen_func_1538(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1538, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1539(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1539 + B
end

function gen_func_1540(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1540
  end
  return sum + (a or 0)
end

function gen_func_1541(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1541, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1542(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1542 + B
end

function gen_func_1543(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1543
  end
  return sum + (a or 0)
end

function gen_func_1544(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1544, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1545(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1545 + B
end

function gen_func_1546(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1546
  end
  return sum + (a or 0)
end

function gen_func_1547(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1547, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1548(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1548 + B
end

function gen_func_1549(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1549
  end
  return sum + (a or 0)
end

function gen_func_1550(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1550, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1551(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1551 + B
end

function gen_func_1552(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1552
  end
  return sum + (a or 0)
end

function gen_func_1553(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1553, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1554(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1554 + B
end

function gen_func_1555(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1555
  end
  return sum + (a or 0)
end

function gen_func_1556(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1556, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1557(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1557 + B
end

function gen_func_1558(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1558
  end
  return sum + (a or 0)
end

function gen_func_1559(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1559, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1560(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1560 + B
end

function gen_func_1561(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1561
  end
  return sum + (a or 0)
end

function gen_func_1562(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1562, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1563(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1563 + B
end

function gen_func_1564(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1564
  end
  return sum + (a or 0)
end

function gen_func_1565(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1565, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1566(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1566 + B
end

function gen_func_1567(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1567
  end
  return sum + (a or 0)
end

function gen_func_1568(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1568, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1569(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1569 + B
end

function gen_func_1570(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1570
  end
  return sum + (a or 0)
end

function gen_func_1571(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1571, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1572(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1572 + B
end

function gen_func_1573(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1573
  end
  return sum + (a or 0)
end

function gen_func_1574(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1574, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1575(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1575 + B
end

function gen_func_1576(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1576
  end
  return sum + (a or 0)
end

function gen_func_1577(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1577, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1578(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1578 + B
end

function gen_func_1579(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1579
  end
  return sum + (a or 0)
end

function gen_func_1580(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1580, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1581(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1581 + B
end

function gen_func_1582(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1582
  end
  return sum + (a or 0)
end

function gen_func_1583(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1583, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1584(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1584 + B
end

function gen_func_1585(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1585
  end
  return sum + (a or 0)
end

function gen_func_1586(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1586, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1587(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1587 + B
end

function gen_func_1588(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1588
  end
  return sum + (a or 0)
end

function gen_func_1589(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1589, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1590(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1590 + B
end

function gen_func_1591(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1591
  end
  return sum + (a or 0)
end

function gen_func_1592(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1592, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1593(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1593 + B
end

function gen_func_1594(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1594
  end
  return sum + (a or 0)
end

function gen_func_1595(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1595, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1596(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1596 + B
end

function gen_func_1597(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1597
  end
  return sum + (a or 0)
end

function gen_func_1598(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1598, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1599(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1599 + B
end

function gen_func_1600(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1600
  end
  return sum + (a or 0)
end

function gen_func_1601(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1601, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1602(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1602 + B
end

function gen_func_1603(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1603
  end
  return sum + (a or 0)
end

function gen_func_1604(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1604, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1605(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1605 + B
end

function gen_func_1606(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1606
  end
  return sum + (a or 0)
end

function gen_func_1607(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1607, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1608(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1608 + B
end

function gen_func_1609(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1609
  end
  return sum + (a or 0)
end

function gen_func_1610(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1610, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1611(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1611 + B
end

function gen_func_1612(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1612
  end
  return sum + (a or 0)
end

function gen_func_1613(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1613, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1614(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1614 + B
end

function gen_func_1615(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1615
  end
  return sum + (a or 0)
end

function gen_func_1616(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1616, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1617(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1617 + B
end

function gen_func_1618(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1618
  end
  return sum + (a or 0)
end

function gen_func_1619(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1619, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1620(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1620 + B
end

function gen_func_1621(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1621
  end
  return sum + (a or 0)
end

function gen_func_1622(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1622, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1623(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1623 + B
end

function gen_func_1624(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1624
  end
  return sum + (a or 0)
end

function gen_func_1625(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1625, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1626(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1626 + B
end

function gen_func_1627(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1627
  end
  return sum + (a or 0)
end

function gen_func_1628(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1628, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1629(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1629 + B
end

function gen_func_1630(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1630
  end
  return sum + (a or 0)
end

function gen_func_1631(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1631, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1632(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1632 + B
end

function gen_func_1633(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1633
  end
  return sum + (a or 0)
end

function gen_func_1634(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1634, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1635(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1635 + B
end

function gen_func_1636(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1636
  end
  return sum + (a or 0)
end

function gen_func_1637(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1637, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1638(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1638 + B
end

function gen_func_1639(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1639
  end
  return sum + (a or 0)
end

function gen_func_1640(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1640, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1641(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1641 + B
end

function gen_func_1642(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1642
  end
  return sum + (a or 0)
end

function gen_func_1643(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1643, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1644(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1644 + B
end

function gen_func_1645(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1645
  end
  return sum + (a or 0)
end

function gen_func_1646(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1646, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1647(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1647 + B
end

function gen_func_1648(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1648
  end
  return sum + (a or 0)
end

function gen_func_1649(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1649, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1650(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1650 + B
end

function gen_func_1651(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1651
  end
  return sum + (a or 0)
end

function gen_func_1652(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1652, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1653(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1653 + B
end

function gen_func_1654(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1654
  end
  return sum + (a or 0)
end

function gen_func_1655(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1655, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1656(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1656 + B
end

function gen_func_1657(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1657
  end
  return sum + (a or 0)
end

function gen_func_1658(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1658, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1659(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1659 + B
end

function gen_func_1660(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1660
  end
  return sum + (a or 0)
end

function gen_func_1661(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1661, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1662(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1662 + B
end

function gen_func_1663(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1663
  end
  return sum + (a or 0)
end

function gen_func_1664(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1664, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1665(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1665 + B
end

function gen_func_1666(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1666
  end
  return sum + (a or 0)
end

function gen_func_1667(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1667, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1668(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1668 + B
end

function gen_func_1669(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1669
  end
  return sum + (a or 0)
end

function gen_func_1670(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1670, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1671(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1671 + B
end

function gen_func_1672(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1672
  end
  return sum + (a or 0)
end

function gen_func_1673(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1673, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1674(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1674 + B
end

function gen_func_1675(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1675
  end
  return sum + (a or 0)
end

function gen_func_1676(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1676, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1677(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1677 + B
end

function gen_func_1678(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1678
  end
  return sum + (a or 0)
end

function gen_func_1679(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1679, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1680(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1680 + B
end

function gen_func_1681(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1681
  end
  return sum + (a or 0)
end

function gen_func_1682(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1682, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1683(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1683 + B
end

function gen_func_1684(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1684
  end
  return sum + (a or 0)
end

function gen_func_1685(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1685, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1686(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1686 + B
end

function gen_func_1687(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1687
  end
  return sum + (a or 0)
end

function gen_func_1688(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1688, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1689(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1689 + B
end

function gen_func_1690(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1690
  end
  return sum + (a or 0)
end

function gen_func_1691(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1691, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1692(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1692 + B
end

function gen_func_1693(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1693
  end
  return sum + (a or 0)
end

function gen_func_1694(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1694, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1695(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1695 + B
end

function gen_func_1696(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1696
  end
  return sum + (a or 0)
end

function gen_func_1697(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1697, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1698(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1698 + B
end

function gen_func_1699(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1699
  end
  return sum + (a or 0)
end

function gen_func_1700(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1700, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1701(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1701 + B
end

function gen_func_1702(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1702
  end
  return sum + (a or 0)
end

function gen_func_1703(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1703, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1704(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1704 + B
end

function gen_func_1705(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1705
  end
  return sum + (a or 0)
end

function gen_func_1706(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1706, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1707(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1707 + B
end

function gen_func_1708(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1708
  end
  return sum + (a or 0)
end

function gen_func_1709(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1709, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1710(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1710 + B
end

function gen_func_1711(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1711
  end
  return sum + (a or 0)
end

function gen_func_1712(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1712, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1713(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1713 + B
end

function gen_func_1714(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1714
  end
  return sum + (a or 0)
end

function gen_func_1715(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1715, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1716(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1716 + B
end

function gen_func_1717(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1717
  end
  return sum + (a or 0)
end

function gen_func_1718(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1718, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1719(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1719 + B
end

function gen_func_1720(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1720
  end
  return sum + (a or 0)
end

function gen_func_1721(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1721, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1722(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1722 + B
end

function gen_func_1723(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1723
  end
  return sum + (a or 0)
end

function gen_func_1724(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1724, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1725(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1725 + B
end

function gen_func_1726(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1726
  end
  return sum + (a or 0)
end

function gen_func_1727(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1727, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1728(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1728 + B
end

function gen_func_1729(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1729
  end
  return sum + (a or 0)
end

function gen_func_1730(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1730, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1731(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1731 + B
end

function gen_func_1732(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1732
  end
  return sum + (a or 0)
end

function gen_func_1733(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1733, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1734(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1734 + B
end

function gen_func_1735(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1735
  end
  return sum + (a or 0)
end

function gen_func_1736(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1736, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1737(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1737 + B
end

function gen_func_1738(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1738
  end
  return sum + (a or 0)
end

function gen_func_1739(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1739, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1740(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1740 + B
end

function gen_func_1741(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1741
  end
  return sum + (a or 0)
end

function gen_func_1742(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1742, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1743(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1743 + B
end

function gen_func_1744(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1744
  end
  return sum + (a or 0)
end

function gen_func_1745(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1745, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1746(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1746 + B
end

function gen_func_1747(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1747
  end
  return sum + (a or 0)
end

function gen_func_1748(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1748, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1749(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1749 + B
end

function gen_func_1750(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1750
  end
  return sum + (a or 0)
end

function gen_func_1751(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1751, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1752(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1752 + B
end

function gen_func_1753(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1753
  end
  return sum + (a or 0)
end

function gen_func_1754(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1754, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1755(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1755 + B
end

function gen_func_1756(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1756
  end
  return sum + (a or 0)
end

function gen_func_1757(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1757, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1758(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1758 + B
end

function gen_func_1759(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1759
  end
  return sum + (a or 0)
end

function gen_func_1760(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1760, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1761(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1761 + B
end

function gen_func_1762(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1762
  end
  return sum + (a or 0)
end

function gen_func_1763(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1763, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1764(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1764 + B
end

function gen_func_1765(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1765
  end
  return sum + (a or 0)
end

function gen_func_1766(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1766, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1767(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1767 + B
end

function gen_func_1768(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1768
  end
  return sum + (a or 0)
end

function gen_func_1769(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1769, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1770(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1770 + B
end

function gen_func_1771(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1771
  end
  return sum + (a or 0)
end

function gen_func_1772(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1772, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1773(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1773 + B
end

function gen_func_1774(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1774
  end
  return sum + (a or 0)
end

function gen_func_1775(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1775, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1776(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1776 + B
end

function gen_func_1777(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1777
  end
  return sum + (a or 0)
end

function gen_func_1778(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1778, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1779(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1779 + B
end

function gen_func_1780(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1780
  end
  return sum + (a or 0)
end

function gen_func_1781(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1781, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1782(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1782 + B
end

function gen_func_1783(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1783
  end
  return sum + (a or 0)
end

function gen_func_1784(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1784, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1785(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1785 + B
end

function gen_func_1786(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1786
  end
  return sum + (a or 0)
end

function gen_func_1787(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1787, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1788(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1788 + B
end

function gen_func_1789(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1789
  end
  return sum + (a or 0)
end

function gen_func_1790(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1790, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1791(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1791 + B
end

function gen_func_1792(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1792
  end
  return sum + (a or 0)
end

function gen_func_1793(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1793, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1794(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1794 + B
end

function gen_func_1795(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1795
  end
  return sum + (a or 0)
end

function gen_func_1796(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1796, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1797(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1797 + B
end

function gen_func_1798(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1798
  end
  return sum + (a or 0)
end

function gen_func_1799(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1799, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1800(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1800 + B
end

function gen_func_1801(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1801
  end
  return sum + (a or 0)
end

function gen_func_1802(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1802, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1803(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1803 + B
end

function gen_func_1804(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1804
  end
  return sum + (a or 0)
end

function gen_func_1805(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1805, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1806(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1806 + B
end

function gen_func_1807(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1807
  end
  return sum + (a or 0)
end

function gen_func_1808(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1808, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1809(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1809 + B
end

function gen_func_1810(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1810
  end
  return sum + (a or 0)
end

function gen_func_1811(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1811, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1812(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1812 + B
end

function gen_func_1813(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1813
  end
  return sum + (a or 0)
end

function gen_func_1814(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1814, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1815(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1815 + B
end

function gen_func_1816(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1816
  end
  return sum + (a or 0)
end

function gen_func_1817(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1817, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1818(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1818 + B
end

function gen_func_1819(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1819
  end
  return sum + (a or 0)
end

function gen_func_1820(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1820, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1821(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1821 + B
end

function gen_func_1822(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1822
  end
  return sum + (a or 0)
end

function gen_func_1823(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1823, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1824(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1824 + B
end

function gen_func_1825(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1825
  end
  return sum + (a or 0)
end

function gen_func_1826(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1826, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1827(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1827 + B
end

function gen_func_1828(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1828
  end
  return sum + (a or 0)
end

function gen_func_1829(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1829, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1830(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1830 + B
end

function gen_func_1831(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1831
  end
  return sum + (a or 0)
end

function gen_func_1832(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1832, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1833(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1833 + B
end

function gen_func_1834(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1834
  end
  return sum + (a or 0)
end

function gen_func_1835(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1835, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1836(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1836 + B
end

function gen_func_1837(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1837
  end
  return sum + (a or 0)
end

function gen_func_1838(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1838, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1839(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1839 + B
end

function gen_func_1840(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1840
  end
  return sum + (a or 0)
end

function gen_func_1841(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1841, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1842(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1842 + B
end

function gen_func_1843(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1843
  end
  return sum + (a or 0)
end

function gen_func_1844(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1844, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1845(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1845 + B
end

function gen_func_1846(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1846
  end
  return sum + (a or 0)
end

function gen_func_1847(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1847, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1848(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1848 + B
end

function gen_func_1849(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1849
  end
  return sum + (a or 0)
end

function gen_func_1850(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1850, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1851(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1851 + B
end

function gen_func_1852(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1852
  end
  return sum + (a or 0)
end

function gen_func_1853(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1853, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1854(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1854 + B
end

function gen_func_1855(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1855
  end
  return sum + (a or 0)
end

function gen_func_1856(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1856, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1857(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1857 + B
end

function gen_func_1858(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1858
  end
  return sum + (a or 0)
end

function gen_func_1859(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1859, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1860(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1860 + B
end

function gen_func_1861(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1861
  end
  return sum + (a or 0)
end

function gen_func_1862(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1862, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1863(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1863 + B
end

function gen_func_1864(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1864
  end
  return sum + (a or 0)
end

function gen_func_1865(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1865, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1866(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1866 + B
end

function gen_func_1867(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1867
  end
  return sum + (a or 0)
end

function gen_func_1868(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1868, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1869(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1869 + B
end

function gen_func_1870(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1870
  end
  return sum + (a or 0)
end

function gen_func_1871(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1871, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1872(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1872 + B
end

function gen_func_1873(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1873
  end
  return sum + (a or 0)
end

function gen_func_1874(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1874, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1875(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1875 + B
end

function gen_func_1876(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1876
  end
  return sum + (a or 0)
end

function gen_func_1877(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1877, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1878(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1878 + B
end

function gen_func_1879(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1879
  end
  return sum + (a or 0)
end

function gen_func_1880(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1880, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1881(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1881 + B
end

function gen_func_1882(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1882
  end
  return sum + (a or 0)
end

function gen_func_1883(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1883, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1884(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1884 + B
end

function gen_func_1885(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1885
  end
  return sum + (a or 0)
end

function gen_func_1886(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1886, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1887(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1887 + B
end

function gen_func_1888(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1888
  end
  return sum + (a or 0)
end

function gen_func_1889(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1889, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1890(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1890 + B
end

function gen_func_1891(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1891
  end
  return sum + (a or 0)
end

function gen_func_1892(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1892, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1893(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1893 + B
end

function gen_func_1894(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1894
  end
  return sum + (a or 0)
end

function gen_func_1895(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1895, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1896(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1896 + B
end

function gen_func_1897(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1897
  end
  return sum + (a or 0)
end

function gen_func_1898(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1898, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1899(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1899 + B
end

function gen_func_1900(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1900
  end
  return sum + (a or 0)
end

function gen_func_1901(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1901, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1902(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1902 + B
end

function gen_func_1903(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1903
  end
  return sum + (a or 0)
end

function gen_func_1904(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1904, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1905(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1905 + B
end

function gen_func_1906(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1906
  end
  return sum + (a or 0)
end

function gen_func_1907(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1907, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1908(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1908 + B
end

function gen_func_1909(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1909
  end
  return sum + (a or 0)
end

function gen_func_1910(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1910, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1911(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1911 + B
end

function gen_func_1912(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1912
  end
  return sum + (a or 0)
end

function gen_func_1913(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1913, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1914(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1914 + B
end

function gen_func_1915(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1915
  end
  return sum + (a or 0)
end

function gen_func_1916(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1916, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1917(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1917 + B
end

function gen_func_1918(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1918
  end
  return sum + (a or 0)
end

function gen_func_1919(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1919, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1920(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1920 + B
end

function gen_func_1921(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1921
  end
  return sum + (a or 0)
end

function gen_func_1922(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1922, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1923(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1923 + B
end

function gen_func_1924(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1924
  end
  return sum + (a or 0)
end

function gen_func_1925(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1925, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1926(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1926 + B
end

function gen_func_1927(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1927
  end
  return sum + (a or 0)
end

function gen_func_1928(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1928, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1929(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1929 + B
end

function gen_func_1930(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1930
  end
  return sum + (a or 0)
end

function gen_func_1931(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1931, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1932(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1932 + B
end

function gen_func_1933(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1933
  end
  return sum + (a or 0)
end

function gen_func_1934(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1934, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1935(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1935 + B
end

function gen_func_1936(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1936
  end
  return sum + (a or 0)
end

function gen_func_1937(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1937, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1938(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1938 + B
end

function gen_func_1939(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1939
  end
  return sum + (a or 0)
end

function gen_func_1940(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1940, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1941(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1941 + B
end

function gen_func_1942(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1942
  end
  return sum + (a or 0)
end

function gen_func_1943(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1943, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1944(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1944 + B
end

function gen_func_1945(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1945
  end
  return sum + (a or 0)
end

function gen_func_1946(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1946, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1947(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1947 + B
end

function gen_func_1948(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1948
  end
  return sum + (a or 0)
end

function gen_func_1949(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1949, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1950(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1950 + B
end

function gen_func_1951(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1951
  end
  return sum + (a or 0)
end

function gen_func_1952(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1952, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1953(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1953 + B
end

function gen_func_1954(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1954
  end
  return sum + (a or 0)
end

function gen_func_1955(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1955, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1956(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1956 + B
end

function gen_func_1957(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1957
  end
  return sum + (a or 0)
end

function gen_func_1958(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1958, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1959(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1959 + B
end

function gen_func_1960(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1960
  end
  return sum + (a or 0)
end

function gen_func_1961(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1961, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1962(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1962 + B
end

function gen_func_1963(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1963
  end
  return sum + (a or 0)
end

function gen_func_1964(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1964, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1965(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1965 + B
end

function gen_func_1966(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1966
  end
  return sum + (a or 0)
end

function gen_func_1967(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1967, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1968(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1968 + B
end

function gen_func_1969(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1969
  end
  return sum + (a or 0)
end

function gen_func_1970(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1970, val = A + 0, ok = (b ~= nil) }
end

function gen_func_1971(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1971 + B
end

function gen_func_1972(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1972
  end
  return sum + (a or 0)
end

function gen_func_1973(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1973, val = A + 3, ok = (b ~= nil) }
end

function gen_func_1974(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1974 + B
end

function gen_func_1975(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1975
  end
  return sum + (a or 0)
end

function gen_func_1976(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1976, val = A + 6, ok = (b ~= nil) }
end

function gen_func_1977(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1977 + B
end

function gen_func_1978(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1978
  end
  return sum + (a or 0)
end

function gen_func_1979(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1979, val = A + 9, ok = (b ~= nil) }
end

function gen_func_1980(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1980 + B
end

function gen_func_1981(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1981
  end
  return sum + (a or 0)
end

function gen_func_1982(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1982, val = A + 2, ok = (b ~= nil) }
end

function gen_func_1983(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1983 + B
end

function gen_func_1984(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1984
  end
  return sum + (a or 0)
end

function gen_func_1985(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1985, val = A + 5, ok = (b ~= nil) }
end

function gen_func_1986(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1986 + B
end

function gen_func_1987(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 1987
  end
  return sum + (a or 0)
end

function gen_func_1988(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1988, val = A + 8, ok = (b ~= nil) }
end

function gen_func_1989(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1989 + B
end

function gen_func_1990(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1990
  end
  return sum + (a or 0)
end

function gen_func_1991(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1991, val = A + 1, ok = (b ~= nil) }
end

function gen_func_1992(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1992 + B
end

function gen_func_1993(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 1993
  end
  return sum + (a or 0)
end

function gen_func_1994(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1994, val = A + 4, ok = (b ~= nil) }
end

function gen_func_1995(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1995 + B
end

function gen_func_1996(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1996
  end
  return sum + (a or 0)
end

function gen_func_1997(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 1997, val = A + 7, ok = (b ~= nil) }
end

function gen_func_1998(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 1998 + B
end

function gen_func_1999(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 1999
  end
  return sum + (a or 0)
end

function gen_func_2000(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2000, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2001(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2001 + B
end

function gen_func_2002(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2002
  end
  return sum + (a or 0)
end

function gen_func_2003(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2003, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2004(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2004 + B
end

function gen_func_2005(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2005
  end
  return sum + (a or 0)
end

function gen_func_2006(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2006, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2007(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2007 + B
end

function gen_func_2008(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2008
  end
  return sum + (a or 0)
end

function gen_func_2009(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2009, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2010(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2010 + B
end

function gen_func_2011(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2011
  end
  return sum + (a or 0)
end

function gen_func_2012(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2012, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2013(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2013 + B
end

function gen_func_2014(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2014
  end
  return sum + (a or 0)
end

function gen_func_2015(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2015, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2016(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2016 + B
end

function gen_func_2017(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2017
  end
  return sum + (a or 0)
end

function gen_func_2018(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2018, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2019(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2019 + B
end

function gen_func_2020(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2020
  end
  return sum + (a or 0)
end

function gen_func_2021(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2021, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2022(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2022 + B
end

function gen_func_2023(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2023
  end
  return sum + (a or 0)
end

function gen_func_2024(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2024, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2025(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2025 + B
end

function gen_func_2026(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2026
  end
  return sum + (a or 0)
end

function gen_func_2027(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2027, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2028(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2028 + B
end

function gen_func_2029(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2029
  end
  return sum + (a or 0)
end

function gen_func_2030(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2030, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2031(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2031 + B
end

function gen_func_2032(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2032
  end
  return sum + (a or 0)
end

function gen_func_2033(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2033, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2034(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2034 + B
end

function gen_func_2035(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2035
  end
  return sum + (a or 0)
end

function gen_func_2036(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2036, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2037(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2037 + B
end

function gen_func_2038(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2038
  end
  return sum + (a or 0)
end

function gen_func_2039(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2039, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2040(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2040 + B
end

function gen_func_2041(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2041
  end
  return sum + (a or 0)
end

function gen_func_2042(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2042, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2043(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2043 + B
end

function gen_func_2044(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2044
  end
  return sum + (a or 0)
end

function gen_func_2045(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2045, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2046(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2046 + B
end

function gen_func_2047(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2047
  end
  return sum + (a or 0)
end

function gen_func_2048(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2048, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2049(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2049 + B
end

function gen_func_2050(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2050
  end
  return sum + (a or 0)
end

function gen_func_2051(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2051, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2052(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2052 + B
end

function gen_func_2053(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2053
  end
  return sum + (a or 0)
end

function gen_func_2054(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2054, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2055(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2055 + B
end

function gen_func_2056(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2056
  end
  return sum + (a or 0)
end

function gen_func_2057(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2057, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2058(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2058 + B
end

function gen_func_2059(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2059
  end
  return sum + (a or 0)
end

function gen_func_2060(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2060, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2061(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2061 + B
end

function gen_func_2062(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2062
  end
  return sum + (a or 0)
end

function gen_func_2063(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2063, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2064(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2064 + B
end

function gen_func_2065(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2065
  end
  return sum + (a or 0)
end

function gen_func_2066(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2066, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2067(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2067 + B
end

function gen_func_2068(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2068
  end
  return sum + (a or 0)
end

function gen_func_2069(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2069, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2070(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2070 + B
end

function gen_func_2071(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2071
  end
  return sum + (a or 0)
end

function gen_func_2072(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2072, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2073(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2073 + B
end

function gen_func_2074(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2074
  end
  return sum + (a or 0)
end

function gen_func_2075(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2075, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2076(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2076 + B
end

function gen_func_2077(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2077
  end
  return sum + (a or 0)
end

function gen_func_2078(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2078, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2079(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2079 + B
end

function gen_func_2080(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2080
  end
  return sum + (a or 0)
end

function gen_func_2081(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2081, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2082(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2082 + B
end

function gen_func_2083(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2083
  end
  return sum + (a or 0)
end

function gen_func_2084(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2084, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2085(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2085 + B
end

function gen_func_2086(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2086
  end
  return sum + (a or 0)
end

function gen_func_2087(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2087, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2088(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2088 + B
end

function gen_func_2089(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2089
  end
  return sum + (a or 0)
end

function gen_func_2090(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2090, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2091(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2091 + B
end

function gen_func_2092(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2092
  end
  return sum + (a or 0)
end

function gen_func_2093(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2093, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2094(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2094 + B
end

function gen_func_2095(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2095
  end
  return sum + (a or 0)
end

function gen_func_2096(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2096, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2097(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2097 + B
end

function gen_func_2098(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2098
  end
  return sum + (a or 0)
end

function gen_func_2099(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2099, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2100(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2100 + B
end

function gen_func_2101(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2101
  end
  return sum + (a or 0)
end

function gen_func_2102(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2102, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2103(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2103 + B
end

function gen_func_2104(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2104
  end
  return sum + (a or 0)
end

function gen_func_2105(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2105, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2106(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2106 + B
end

function gen_func_2107(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2107
  end
  return sum + (a or 0)
end

function gen_func_2108(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2108, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2109(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2109 + B
end

function gen_func_2110(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2110
  end
  return sum + (a or 0)
end

function gen_func_2111(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2111, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2112(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2112 + B
end

function gen_func_2113(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2113
  end
  return sum + (a or 0)
end

function gen_func_2114(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2114, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2115(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2115 + B
end

function gen_func_2116(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2116
  end
  return sum + (a or 0)
end

function gen_func_2117(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2117, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2118(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2118 + B
end

function gen_func_2119(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2119
  end
  return sum + (a or 0)
end

function gen_func_2120(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2120, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2121(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2121 + B
end

function gen_func_2122(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2122
  end
  return sum + (a or 0)
end

function gen_func_2123(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2123, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2124(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2124 + B
end

function gen_func_2125(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2125
  end
  return sum + (a or 0)
end

function gen_func_2126(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2126, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2127(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2127 + B
end

function gen_func_2128(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2128
  end
  return sum + (a or 0)
end

function gen_func_2129(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2129, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2130(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2130 + B
end

function gen_func_2131(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2131
  end
  return sum + (a or 0)
end

function gen_func_2132(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2132, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2133(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2133 + B
end

function gen_func_2134(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2134
  end
  return sum + (a or 0)
end

function gen_func_2135(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2135, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2136(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2136 + B
end

function gen_func_2137(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2137
  end
  return sum + (a or 0)
end

function gen_func_2138(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2138, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2139(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2139 + B
end

function gen_func_2140(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2140
  end
  return sum + (a or 0)
end

function gen_func_2141(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2141, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2142(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2142 + B
end

function gen_func_2143(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2143
  end
  return sum + (a or 0)
end

function gen_func_2144(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2144, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2145(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2145 + B
end

function gen_func_2146(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2146
  end
  return sum + (a or 0)
end

function gen_func_2147(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2147, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2148(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2148 + B
end

function gen_func_2149(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2149
  end
  return sum + (a or 0)
end

function gen_func_2150(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2150, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2151(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2151 + B
end

function gen_func_2152(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2152
  end
  return sum + (a or 0)
end

function gen_func_2153(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2153, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2154(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2154 + B
end

function gen_func_2155(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2155
  end
  return sum + (a or 0)
end

function gen_func_2156(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2156, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2157(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2157 + B
end

function gen_func_2158(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2158
  end
  return sum + (a or 0)
end

function gen_func_2159(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2159, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2160(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2160 + B
end

function gen_func_2161(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2161
  end
  return sum + (a or 0)
end

function gen_func_2162(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2162, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2163(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2163 + B
end

function gen_func_2164(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2164
  end
  return sum + (a or 0)
end

function gen_func_2165(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2165, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2166(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2166 + B
end

function gen_func_2167(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2167
  end
  return sum + (a or 0)
end

function gen_func_2168(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2168, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2169(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2169 + B
end

function gen_func_2170(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2170
  end
  return sum + (a or 0)
end

function gen_func_2171(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2171, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2172(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2172 + B
end

function gen_func_2173(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2173
  end
  return sum + (a or 0)
end

function gen_func_2174(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2174, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2175(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2175 + B
end

function gen_func_2176(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2176
  end
  return sum + (a or 0)
end

function gen_func_2177(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2177, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2178(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2178 + B
end

function gen_func_2179(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2179
  end
  return sum + (a or 0)
end

function gen_func_2180(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2180, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2181(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2181 + B
end

function gen_func_2182(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2182
  end
  return sum + (a or 0)
end

function gen_func_2183(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2183, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2184(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2184 + B
end

function gen_func_2185(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2185
  end
  return sum + (a or 0)
end

function gen_func_2186(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2186, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2187(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2187 + B
end

function gen_func_2188(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2188
  end
  return sum + (a or 0)
end

function gen_func_2189(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2189, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2190(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2190 + B
end

function gen_func_2191(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2191
  end
  return sum + (a or 0)
end

function gen_func_2192(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2192, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2193(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2193 + B
end

function gen_func_2194(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2194
  end
  return sum + (a or 0)
end

function gen_func_2195(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2195, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2196(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2196 + B
end

function gen_func_2197(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2197
  end
  return sum + (a or 0)
end

function gen_func_2198(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2198, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2199(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2199 + B
end

function gen_func_2200(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2200
  end
  return sum + (a or 0)
end

function gen_func_2201(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2201, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2202(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2202 + B
end

function gen_func_2203(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2203
  end
  return sum + (a or 0)
end

function gen_func_2204(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2204, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2205(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2205 + B
end

function gen_func_2206(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2206
  end
  return sum + (a or 0)
end

function gen_func_2207(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2207, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2208(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2208 + B
end

function gen_func_2209(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2209
  end
  return sum + (a or 0)
end

function gen_func_2210(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2210, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2211(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2211 + B
end

function gen_func_2212(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2212
  end
  return sum + (a or 0)
end

function gen_func_2213(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2213, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2214(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2214 + B
end

function gen_func_2215(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2215
  end
  return sum + (a or 0)
end

function gen_func_2216(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2216, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2217(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2217 + B
end

function gen_func_2218(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2218
  end
  return sum + (a or 0)
end

function gen_func_2219(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2219, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2220(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2220 + B
end

function gen_func_2221(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2221
  end
  return sum + (a or 0)
end

function gen_func_2222(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2222, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2223(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2223 + B
end

function gen_func_2224(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2224
  end
  return sum + (a or 0)
end

function gen_func_2225(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2225, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2226(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2226 + B
end

function gen_func_2227(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2227
  end
  return sum + (a or 0)
end

function gen_func_2228(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2228, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2229(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2229 + B
end

function gen_func_2230(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2230
  end
  return sum + (a or 0)
end

function gen_func_2231(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2231, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2232(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2232 + B
end

function gen_func_2233(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2233
  end
  return sum + (a or 0)
end

function gen_func_2234(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2234, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2235(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2235 + B
end

function gen_func_2236(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2236
  end
  return sum + (a or 0)
end

function gen_func_2237(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2237, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2238(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2238 + B
end

function gen_func_2239(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2239
  end
  return sum + (a or 0)
end

function gen_func_2240(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2240, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2241(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2241 + B
end

function gen_func_2242(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2242
  end
  return sum + (a or 0)
end

function gen_func_2243(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2243, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2244(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2244 + B
end

function gen_func_2245(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2245
  end
  return sum + (a or 0)
end

function gen_func_2246(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2246, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2247(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2247 + B
end

function gen_func_2248(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2248
  end
  return sum + (a or 0)
end

function gen_func_2249(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2249, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2250(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2250 + B
end

function gen_func_2251(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2251
  end
  return sum + (a or 0)
end

function gen_func_2252(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2252, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2253(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2253 + B
end

function gen_func_2254(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2254
  end
  return sum + (a or 0)
end

function gen_func_2255(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2255, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2256(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2256 + B
end

function gen_func_2257(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2257
  end
  return sum + (a or 0)
end

function gen_func_2258(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2258, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2259(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2259 + B
end

function gen_func_2260(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2260
  end
  return sum + (a or 0)
end

function gen_func_2261(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2261, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2262(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2262 + B
end

function gen_func_2263(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2263
  end
  return sum + (a or 0)
end

function gen_func_2264(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2264, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2265(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2265 + B
end

function gen_func_2266(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2266
  end
  return sum + (a or 0)
end

function gen_func_2267(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2267, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2268(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2268 + B
end

function gen_func_2269(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2269
  end
  return sum + (a or 0)
end

function gen_func_2270(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2270, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2271(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2271 + B
end

function gen_func_2272(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2272
  end
  return sum + (a or 0)
end

function gen_func_2273(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2273, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2274(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2274 + B
end

function gen_func_2275(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2275
  end
  return sum + (a or 0)
end

function gen_func_2276(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2276, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2277(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2277 + B
end

function gen_func_2278(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2278
  end
  return sum + (a or 0)
end

function gen_func_2279(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2279, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2280(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2280 + B
end

function gen_func_2281(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2281
  end
  return sum + (a or 0)
end

function gen_func_2282(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2282, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2283(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2283 + B
end

function gen_func_2284(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2284
  end
  return sum + (a or 0)
end

function gen_func_2285(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2285, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2286(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2286 + B
end

function gen_func_2287(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2287
  end
  return sum + (a or 0)
end

function gen_func_2288(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2288, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2289(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2289 + B
end

function gen_func_2290(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2290
  end
  return sum + (a or 0)
end

function gen_func_2291(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2291, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2292(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2292 + B
end

function gen_func_2293(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2293
  end
  return sum + (a or 0)
end

function gen_func_2294(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2294, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2295(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2295 + B
end

function gen_func_2296(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2296
  end
  return sum + (a or 0)
end

function gen_func_2297(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2297, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2298(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2298 + B
end

function gen_func_2299(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2299
  end
  return sum + (a or 0)
end

function gen_func_2300(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2300, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2301(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2301 + B
end

function gen_func_2302(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2302
  end
  return sum + (a or 0)
end

function gen_func_2303(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2303, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2304(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2304 + B
end

function gen_func_2305(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2305
  end
  return sum + (a or 0)
end

function gen_func_2306(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2306, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2307(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2307 + B
end

function gen_func_2308(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2308
  end
  return sum + (a or 0)
end

function gen_func_2309(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2309, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2310(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2310 + B
end

function gen_func_2311(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2311
  end
  return sum + (a or 0)
end

function gen_func_2312(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2312, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2313(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2313 + B
end

function gen_func_2314(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2314
  end
  return sum + (a or 0)
end

function gen_func_2315(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2315, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2316(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2316 + B
end

function gen_func_2317(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2317
  end
  return sum + (a or 0)
end

function gen_func_2318(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2318, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2319(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2319 + B
end

function gen_func_2320(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2320
  end
  return sum + (a or 0)
end

function gen_func_2321(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2321, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2322(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2322 + B
end

function gen_func_2323(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2323
  end
  return sum + (a or 0)
end

function gen_func_2324(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2324, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2325(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2325 + B
end

function gen_func_2326(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2326
  end
  return sum + (a or 0)
end

function gen_func_2327(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2327, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2328(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2328 + B
end

function gen_func_2329(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2329
  end
  return sum + (a or 0)
end

function gen_func_2330(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2330, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2331(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2331 + B
end

function gen_func_2332(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2332
  end
  return sum + (a or 0)
end

function gen_func_2333(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2333, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2334(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2334 + B
end

function gen_func_2335(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2335
  end
  return sum + (a or 0)
end

function gen_func_2336(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2336, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2337(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2337 + B
end

function gen_func_2338(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2338
  end
  return sum + (a or 0)
end

function gen_func_2339(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2339, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2340(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2340 + B
end

function gen_func_2341(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2341
  end
  return sum + (a or 0)
end

function gen_func_2342(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2342, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2343(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2343 + B
end

function gen_func_2344(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2344
  end
  return sum + (a or 0)
end

function gen_func_2345(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2345, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2346(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2346 + B
end

function gen_func_2347(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2347
  end
  return sum + (a or 0)
end

function gen_func_2348(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2348, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2349(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2349 + B
end

function gen_func_2350(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2350
  end
  return sum + (a or 0)
end

function gen_func_2351(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2351, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2352(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2352 + B
end

function gen_func_2353(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2353
  end
  return sum + (a or 0)
end

function gen_func_2354(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2354, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2355(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2355 + B
end

function gen_func_2356(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2356
  end
  return sum + (a or 0)
end

function gen_func_2357(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2357, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2358(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2358 + B
end

function gen_func_2359(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2359
  end
  return sum + (a or 0)
end

function gen_func_2360(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2360, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2361(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2361 + B
end

function gen_func_2362(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2362
  end
  return sum + (a or 0)
end

function gen_func_2363(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2363, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2364(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2364 + B
end

function gen_func_2365(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2365
  end
  return sum + (a or 0)
end

function gen_func_2366(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2366, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2367(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2367 + B
end

function gen_func_2368(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2368
  end
  return sum + (a or 0)
end

function gen_func_2369(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2369, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2370(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2370 + B
end

function gen_func_2371(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2371
  end
  return sum + (a or 0)
end

function gen_func_2372(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2372, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2373(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2373 + B
end

function gen_func_2374(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2374
  end
  return sum + (a or 0)
end

function gen_func_2375(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2375, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2376(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2376 + B
end

function gen_func_2377(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2377
  end
  return sum + (a or 0)
end

function gen_func_2378(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2378, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2379(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2379 + B
end

function gen_func_2380(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2380
  end
  return sum + (a or 0)
end

function gen_func_2381(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2381, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2382(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2382 + B
end

function gen_func_2383(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2383
  end
  return sum + (a or 0)
end

function gen_func_2384(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2384, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2385(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2385 + B
end

function gen_func_2386(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2386
  end
  return sum + (a or 0)
end

function gen_func_2387(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2387, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2388(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2388 + B
end

function gen_func_2389(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2389
  end
  return sum + (a or 0)
end

function gen_func_2390(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2390, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2391(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2391 + B
end

function gen_func_2392(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2392
  end
  return sum + (a or 0)
end

function gen_func_2393(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2393, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2394(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2394 + B
end

function gen_func_2395(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2395
  end
  return sum + (a or 0)
end

function gen_func_2396(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2396, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2397(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2397 + B
end

function gen_func_2398(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2398
  end
  return sum + (a or 0)
end

function gen_func_2399(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2399, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2400(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2400 + B
end

function gen_func_2401(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2401
  end
  return sum + (a or 0)
end

function gen_func_2402(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2402, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2403(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2403 + B
end

function gen_func_2404(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2404
  end
  return sum + (a or 0)
end

function gen_func_2405(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2405, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2406(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2406 + B
end

function gen_func_2407(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2407
  end
  return sum + (a or 0)
end

function gen_func_2408(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2408, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2409(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2409 + B
end

function gen_func_2410(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2410
  end
  return sum + (a or 0)
end

function gen_func_2411(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2411, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2412(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2412 + B
end

function gen_func_2413(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2413
  end
  return sum + (a or 0)
end

function gen_func_2414(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2414, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2415(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2415 + B
end

function gen_func_2416(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2416
  end
  return sum + (a or 0)
end

function gen_func_2417(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2417, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2418(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2418 + B
end

function gen_func_2419(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2419
  end
  return sum + (a or 0)
end

function gen_func_2420(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2420, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2421(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2421 + B
end

function gen_func_2422(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2422
  end
  return sum + (a or 0)
end

function gen_func_2423(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2423, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2424(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2424 + B
end

function gen_func_2425(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2425
  end
  return sum + (a or 0)
end

function gen_func_2426(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2426, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2427(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2427 + B
end

function gen_func_2428(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2428
  end
  return sum + (a or 0)
end

function gen_func_2429(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2429, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2430(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2430 + B
end

function gen_func_2431(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2431
  end
  return sum + (a or 0)
end

function gen_func_2432(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2432, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2433(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2433 + B
end

function gen_func_2434(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2434
  end
  return sum + (a or 0)
end

function gen_func_2435(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2435, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2436(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2436 + B
end

function gen_func_2437(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2437
  end
  return sum + (a or 0)
end

function gen_func_2438(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2438, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2439(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2439 + B
end

function gen_func_2440(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2440
  end
  return sum + (a or 0)
end

function gen_func_2441(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2441, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2442(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2442 + B
end

function gen_func_2443(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2443
  end
  return sum + (a or 0)
end

function gen_func_2444(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2444, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2445(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2445 + B
end

function gen_func_2446(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2446
  end
  return sum + (a or 0)
end

function gen_func_2447(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2447, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2448(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2448 + B
end

function gen_func_2449(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2449
  end
  return sum + (a or 0)
end

function gen_func_2450(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2450, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2451(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2451 + B
end

function gen_func_2452(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2452
  end
  return sum + (a or 0)
end

function gen_func_2453(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2453, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2454(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2454 + B
end

function gen_func_2455(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2455
  end
  return sum + (a or 0)
end

function gen_func_2456(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2456, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2457(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2457 + B
end

function gen_func_2458(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2458
  end
  return sum + (a or 0)
end

function gen_func_2459(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2459, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2460(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2460 + B
end

function gen_func_2461(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2461
  end
  return sum + (a or 0)
end

function gen_func_2462(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2462, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2463(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2463 + B
end

function gen_func_2464(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2464
  end
  return sum + (a or 0)
end

function gen_func_2465(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2465, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2466(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2466 + B
end

function gen_func_2467(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2467
  end
  return sum + (a or 0)
end

function gen_func_2468(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2468, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2469(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2469 + B
end

function gen_func_2470(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2470
  end
  return sum + (a or 0)
end

function gen_func_2471(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2471, val = A + 1, ok = (b ~= nil) }
end

function gen_func_2472(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2472 + B
end

function gen_func_2473(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2473
  end
  return sum + (a or 0)
end

function gen_func_2474(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2474, val = A + 4, ok = (b ~= nil) }
end

function gen_func_2475(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2475 + B
end

function gen_func_2476(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2476
  end
  return sum + (a or 0)
end

function gen_func_2477(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2477, val = A + 7, ok = (b ~= nil) }
end

function gen_func_2478(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2478 + B
end

function gen_func_2479(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2479
  end
  return sum + (a or 0)
end

function gen_func_2480(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2480, val = A + 0, ok = (b ~= nil) }
end

function gen_func_2481(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2481 + B
end

function gen_func_2482(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2482
  end
  return sum + (a or 0)
end

function gen_func_2483(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2483, val = A + 3, ok = (b ~= nil) }
end

function gen_func_2484(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2484 + B
end

function gen_func_2485(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2485
  end
  return sum + (a or 0)
end

function gen_func_2486(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2486, val = A + 6, ok = (b ~= nil) }
end

function gen_func_2487(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2487 + B
end

function gen_func_2488(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 2488
  end
  return sum + (a or 0)
end

function gen_func_2489(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2489, val = A + 9, ok = (b ~= nil) }
end

function gen_func_2490(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2490 + B
end

function gen_func_2491(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 2491
  end
  return sum + (a or 0)
end

function gen_func_2492(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2492, val = A + 2, ok = (b ~= nil) }
end

function gen_func_2493(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2493 + B
end

function gen_func_2494(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 2494
  end
  return sum + (a or 0)
end

function gen_func_2495(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2495, val = A + 5, ok = (b ~= nil) }
end

function gen_func_2496(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2496 + B
end

function gen_func_2497(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 2497
  end
  return sum + (a or 0)
end

function gen_func_2498(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2498, val = A + 8, ok = (b ~= nil) }
end

function gen_func_2499(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 2499 + B
end

function gen_func_2500(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 2500
  end
  return sum + (a or 0)
end

