-- Generated Lua functions (2025-11-22T02:04:08Z)

function gen_func_1(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 1
  end
  return sum + (a or 0)
end

function gen_func_2(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 2, val = A + 2, ok = (b ~= nil) }
end

function gen_func_3(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 3 + B
end

function gen_func_4(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 4
  end
  return sum + (a or 0)
end

function gen_func_5(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 5, val = A + 5, ok = (b ~= nil) }
end

function gen_func_6(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 6 + B
end

function gen_func_7(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 7
  end
  return sum + (a or 0)
end

function gen_func_8(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 8, val = A + 8, ok = (b ~= nil) }
end

function gen_func_9(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 9 + B
end

function gen_func_10(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 10
  end
  return sum + (a or 0)
end

function gen_func_11(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 11, val = A + 1, ok = (b ~= nil) }
end

function gen_func_12(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 12 + B
end

function gen_func_13(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 13
  end
  return sum + (a or 0)
end

function gen_func_14(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 14, val = A + 4, ok = (b ~= nil) }
end

function gen_func_15(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 15 + B
end

function gen_func_16(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 16
  end
  return sum + (a or 0)
end

function gen_func_17(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 17, val = A + 7, ok = (b ~= nil) }
end

function gen_func_18(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 18 + B
end

function gen_func_19(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 19
  end
  return sum + (a or 0)
end

function gen_func_20(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 20, val = A + 0, ok = (b ~= nil) }
end

function gen_func_21(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 21 + B
end

function gen_func_22(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 22
  end
  return sum + (a or 0)
end

function gen_func_23(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 23, val = A + 3, ok = (b ~= nil) }
end

function gen_func_24(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 24 + B
end

function gen_func_25(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 25
  end
  return sum + (a or 0)
end

function gen_func_26(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 26, val = A + 6, ok = (b ~= nil) }
end

function gen_func_27(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 27 + B
end

function gen_func_28(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 28
  end
  return sum + (a or 0)
end

function gen_func_29(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 29, val = A + 9, ok = (b ~= nil) }
end

function gen_func_30(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 30 + B
end

function gen_func_31(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 31
  end
  return sum + (a or 0)
end

function gen_func_32(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 32, val = A + 2, ok = (b ~= nil) }
end

function gen_func_33(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 33 + B
end

function gen_func_34(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 34
  end
  return sum + (a or 0)
end

function gen_func_35(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 35, val = A + 5, ok = (b ~= nil) }
end

function gen_func_36(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 36 + B
end

function gen_func_37(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 37
  end
  return sum + (a or 0)
end

function gen_func_38(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 38, val = A + 8, ok = (b ~= nil) }
end

function gen_func_39(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 39 + B
end

function gen_func_40(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 40
  end
  return sum + (a or 0)
end

function gen_func_41(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 41, val = A + 1, ok = (b ~= nil) }
end

function gen_func_42(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 42 + B
end

function gen_func_43(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 43
  end
  return sum + (a or 0)
end

function gen_func_44(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 44, val = A + 4, ok = (b ~= nil) }
end

function gen_func_45(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 45 + B
end

function gen_func_46(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 46
  end
  return sum + (a or 0)
end

function gen_func_47(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 47, val = A + 7, ok = (b ~= nil) }
end

function gen_func_48(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 48 + B
end

function gen_func_49(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 49
  end
  return sum + (a or 0)
end

function gen_func_50(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 50, val = A + 0, ok = (b ~= nil) }
end

function gen_func_51(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 51 + B
end

function gen_func_52(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 52
  end
  return sum + (a or 0)
end

function gen_func_53(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 53, val = A + 3, ok = (b ~= nil) }
end

function gen_func_54(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 54 + B
end

function gen_func_55(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 55
  end
  return sum + (a or 0)
end

function gen_func_56(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 56, val = A + 6, ok = (b ~= nil) }
end

function gen_func_57(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 57 + B
end

function gen_func_58(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 58
  end
  return sum + (a or 0)
end

function gen_func_59(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 59, val = A + 9, ok = (b ~= nil) }
end

function gen_func_60(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 60 + B
end

function gen_func_61(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 61
  end
  return sum + (a or 0)
end

function gen_func_62(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 62, val = A + 2, ok = (b ~= nil) }
end

function gen_func_63(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 63 + B
end

function gen_func_64(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 64
  end
  return sum + (a or 0)
end

function gen_func_65(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 65, val = A + 5, ok = (b ~= nil) }
end

function gen_func_66(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 66 + B
end

function gen_func_67(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 67
  end
  return sum + (a or 0)
end

function gen_func_68(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 68, val = A + 8, ok = (b ~= nil) }
end

function gen_func_69(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 69 + B
end

function gen_func_70(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 70
  end
  return sum + (a or 0)
end

function gen_func_71(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 71, val = A + 1, ok = (b ~= nil) }
end

function gen_func_72(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 72 + B
end

function gen_func_73(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 73
  end
  return sum + (a or 0)
end

function gen_func_74(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 74, val = A + 4, ok = (b ~= nil) }
end

function gen_func_75(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 75 + B
end

function gen_func_76(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 76
  end
  return sum + (a or 0)
end

function gen_func_77(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 77, val = A + 7, ok = (b ~= nil) }
end

function gen_func_78(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 78 + B
end

function gen_func_79(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 79
  end
  return sum + (a or 0)
end

function gen_func_80(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 80, val = A + 0, ok = (b ~= nil) }
end

function gen_func_81(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 81 + B
end

function gen_func_82(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 82
  end
  return sum + (a or 0)
end

function gen_func_83(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 83, val = A + 3, ok = (b ~= nil) }
end

function gen_func_84(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 84 + B
end

function gen_func_85(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 85
  end
  return sum + (a or 0)
end

function gen_func_86(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 86, val = A + 6, ok = (b ~= nil) }
end

function gen_func_87(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 87 + B
end

function gen_func_88(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 88
  end
  return sum + (a or 0)
end

function gen_func_89(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 89, val = A + 9, ok = (b ~= nil) }
end

function gen_func_90(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 90 + B
end

function gen_func_91(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 91
  end
  return sum + (a or 0)
end

function gen_func_92(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 92, val = A + 2, ok = (b ~= nil) }
end

function gen_func_93(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 93 + B
end

function gen_func_94(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 94
  end
  return sum + (a or 0)
end

function gen_func_95(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 95, val = A + 5, ok = (b ~= nil) }
end

function gen_func_96(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 96 + B
end

function gen_func_97(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 97
  end
  return sum + (a or 0)
end

function gen_func_98(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 98, val = A + 8, ok = (b ~= nil) }
end

function gen_func_99(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 99 + B
end

function gen_func_100(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 100
  end
  return sum + (a or 0)
end

function gen_func_101(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 101, val = A + 1, ok = (b ~= nil) }
end

function gen_func_102(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 102 + B
end

function gen_func_103(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 103
  end
  return sum + (a or 0)
end

function gen_func_104(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 104, val = A + 4, ok = (b ~= nil) }
end

function gen_func_105(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 105 + B
end

function gen_func_106(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 106
  end
  return sum + (a or 0)
end

function gen_func_107(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 107, val = A + 7, ok = (b ~= nil) }
end

function gen_func_108(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 108 + B
end

function gen_func_109(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 109
  end
  return sum + (a or 0)
end

function gen_func_110(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 110, val = A + 0, ok = (b ~= nil) }
end

function gen_func_111(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 111 + B
end

function gen_func_112(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 112
  end
  return sum + (a or 0)
end

function gen_func_113(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 113, val = A + 3, ok = (b ~= nil) }
end

function gen_func_114(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 114 + B
end

function gen_func_115(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 115
  end
  return sum + (a or 0)
end

function gen_func_116(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 116, val = A + 6, ok = (b ~= nil) }
end

function gen_func_117(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 117 + B
end

function gen_func_118(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 118
  end
  return sum + (a or 0)
end

function gen_func_119(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 119, val = A + 9, ok = (b ~= nil) }
end

function gen_func_120(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 120 + B
end

function gen_func_121(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 121
  end
  return sum + (a or 0)
end

function gen_func_122(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 122, val = A + 2, ok = (b ~= nil) }
end

function gen_func_123(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 123 + B
end

function gen_func_124(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 124
  end
  return sum + (a or 0)
end

function gen_func_125(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 125, val = A + 5, ok = (b ~= nil) }
end

function gen_func_126(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 126 + B
end

function gen_func_127(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 127
  end
  return sum + (a or 0)
end

function gen_func_128(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 128, val = A + 8, ok = (b ~= nil) }
end

function gen_func_129(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 129 + B
end

function gen_func_130(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 130
  end
  return sum + (a or 0)
end

function gen_func_131(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 131, val = A + 1, ok = (b ~= nil) }
end

function gen_func_132(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 132 + B
end

function gen_func_133(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 133
  end
  return sum + (a or 0)
end

function gen_func_134(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 134, val = A + 4, ok = (b ~= nil) }
end

function gen_func_135(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 135 + B
end

function gen_func_136(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 136
  end
  return sum + (a or 0)
end

function gen_func_137(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 137, val = A + 7, ok = (b ~= nil) }
end

function gen_func_138(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 138 + B
end

function gen_func_139(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 139
  end
  return sum + (a or 0)
end

function gen_func_140(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 140, val = A + 0, ok = (b ~= nil) }
end

function gen_func_141(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 141 + B
end

function gen_func_142(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 142
  end
  return sum + (a or 0)
end

function gen_func_143(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 143, val = A + 3, ok = (b ~= nil) }
end

function gen_func_144(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 144 + B
end

function gen_func_145(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 145
  end
  return sum + (a or 0)
end

function gen_func_146(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 146, val = A + 6, ok = (b ~= nil) }
end

function gen_func_147(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 147 + B
end

function gen_func_148(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 148
  end
  return sum + (a or 0)
end

function gen_func_149(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 149, val = A + 9, ok = (b ~= nil) }
end

function gen_func_150(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 150 + B
end

function gen_func_151(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 151
  end
  return sum + (a or 0)
end

function gen_func_152(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 152, val = A + 2, ok = (b ~= nil) }
end

function gen_func_153(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 153 + B
end

function gen_func_154(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 154
  end
  return sum + (a or 0)
end

function gen_func_155(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 155, val = A + 5, ok = (b ~= nil) }
end

function gen_func_156(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 156 + B
end

function gen_func_157(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 157
  end
  return sum + (a or 0)
end

function gen_func_158(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 158, val = A + 8, ok = (b ~= nil) }
end

function gen_func_159(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 159 + B
end

function gen_func_160(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 160
  end
  return sum + (a or 0)
end

function gen_func_161(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 161, val = A + 1, ok = (b ~= nil) }
end

function gen_func_162(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 162 + B
end

function gen_func_163(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 163
  end
  return sum + (a or 0)
end

function gen_func_164(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 164, val = A + 4, ok = (b ~= nil) }
end

function gen_func_165(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 165 + B
end

function gen_func_166(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 166
  end
  return sum + (a or 0)
end

function gen_func_167(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 167, val = A + 7, ok = (b ~= nil) }
end

function gen_func_168(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 168 + B
end

function gen_func_169(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 169
  end
  return sum + (a or 0)
end

function gen_func_170(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 170, val = A + 0, ok = (b ~= nil) }
end

function gen_func_171(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 171 + B
end

function gen_func_172(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 172
  end
  return sum + (a or 0)
end

function gen_func_173(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 173, val = A + 3, ok = (b ~= nil) }
end

function gen_func_174(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 174 + B
end

function gen_func_175(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 175
  end
  return sum + (a or 0)
end

function gen_func_176(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 176, val = A + 6, ok = (b ~= nil) }
end

function gen_func_177(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 177 + B
end

function gen_func_178(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 178
  end
  return sum + (a or 0)
end

function gen_func_179(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 179, val = A + 9, ok = (b ~= nil) }
end

function gen_func_180(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 180 + B
end

function gen_func_181(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 181
  end
  return sum + (a or 0)
end

function gen_func_182(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 182, val = A + 2, ok = (b ~= nil) }
end

function gen_func_183(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 183 + B
end

function gen_func_184(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 184
  end
  return sum + (a or 0)
end

function gen_func_185(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 185, val = A + 5, ok = (b ~= nil) }
end

function gen_func_186(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 186 + B
end

function gen_func_187(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 187
  end
  return sum + (a or 0)
end

function gen_func_188(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 188, val = A + 8, ok = (b ~= nil) }
end

function gen_func_189(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 189 + B
end

function gen_func_190(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 190
  end
  return sum + (a or 0)
end

function gen_func_191(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 191, val = A + 1, ok = (b ~= nil) }
end

function gen_func_192(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 192 + B
end

function gen_func_193(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 193
  end
  return sum + (a or 0)
end

function gen_func_194(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 194, val = A + 4, ok = (b ~= nil) }
end

function gen_func_195(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 195 + B
end

function gen_func_196(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 196
  end
  return sum + (a or 0)
end

function gen_func_197(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 197, val = A + 7, ok = (b ~= nil) }
end

function gen_func_198(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 198 + B
end

function gen_func_199(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 199
  end
  return sum + (a or 0)
end

function gen_func_200(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 200, val = A + 0, ok = (b ~= nil) }
end

function gen_func_201(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 201 + B
end

function gen_func_202(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 202
  end
  return sum + (a or 0)
end

function gen_func_203(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 203, val = A + 3, ok = (b ~= nil) }
end

function gen_func_204(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 204 + B
end

function gen_func_205(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 205
  end
  return sum + (a or 0)
end

function gen_func_206(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 206, val = A + 6, ok = (b ~= nil) }
end

function gen_func_207(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 207 + B
end

function gen_func_208(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 208
  end
  return sum + (a or 0)
end

function gen_func_209(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 209, val = A + 9, ok = (b ~= nil) }
end

function gen_func_210(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 210 + B
end

function gen_func_211(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 211
  end
  return sum + (a or 0)
end

function gen_func_212(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 212, val = A + 2, ok = (b ~= nil) }
end

function gen_func_213(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 213 + B
end

function gen_func_214(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 214
  end
  return sum + (a or 0)
end

function gen_func_215(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 215, val = A + 5, ok = (b ~= nil) }
end

function gen_func_216(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 216 + B
end

function gen_func_217(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 217
  end
  return sum + (a or 0)
end

function gen_func_218(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 218, val = A + 8, ok = (b ~= nil) }
end

function gen_func_219(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 219 + B
end

function gen_func_220(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 220
  end
  return sum + (a or 0)
end

function gen_func_221(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 221, val = A + 1, ok = (b ~= nil) }
end

function gen_func_222(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 222 + B
end

function gen_func_223(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 223
  end
  return sum + (a or 0)
end

function gen_func_224(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 224, val = A + 4, ok = (b ~= nil) }
end

function gen_func_225(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 225 + B
end

function gen_func_226(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 226
  end
  return sum + (a or 0)
end

function gen_func_227(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 227, val = A + 7, ok = (b ~= nil) }
end

function gen_func_228(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 228 + B
end

function gen_func_229(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 229
  end
  return sum + (a or 0)
end

function gen_func_230(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 230, val = A + 0, ok = (b ~= nil) }
end

function gen_func_231(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 231 + B
end

function gen_func_232(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 232
  end
  return sum + (a or 0)
end

function gen_func_233(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 233, val = A + 3, ok = (b ~= nil) }
end

function gen_func_234(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 234 + B
end

function gen_func_235(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 235
  end
  return sum + (a or 0)
end

function gen_func_236(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 236, val = A + 6, ok = (b ~= nil) }
end

function gen_func_237(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 237 + B
end

function gen_func_238(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 238
  end
  return sum + (a or 0)
end

function gen_func_239(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 239, val = A + 9, ok = (b ~= nil) }
end

function gen_func_240(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 240 + B
end

function gen_func_241(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 241
  end
  return sum + (a or 0)
end

function gen_func_242(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 242, val = A + 2, ok = (b ~= nil) }
end

function gen_func_243(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 243 + B
end

function gen_func_244(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 244
  end
  return sum + (a or 0)
end

function gen_func_245(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 245, val = A + 5, ok = (b ~= nil) }
end

function gen_func_246(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 246 + B
end

function gen_func_247(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 247
  end
  return sum + (a or 0)
end

function gen_func_248(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 248, val = A + 8, ok = (b ~= nil) }
end

function gen_func_249(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 249 + B
end

function gen_func_250(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 250
  end
  return sum + (a or 0)
end

function gen_func_251(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 251, val = A + 1, ok = (b ~= nil) }
end

function gen_func_252(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 252 + B
end

function gen_func_253(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 253
  end
  return sum + (a or 0)
end

function gen_func_254(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 254, val = A + 4, ok = (b ~= nil) }
end

function gen_func_255(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 255 + B
end

function gen_func_256(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 256
  end
  return sum + (a or 0)
end

function gen_func_257(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 257, val = A + 7, ok = (b ~= nil) }
end

function gen_func_258(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 258 + B
end

function gen_func_259(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 259
  end
  return sum + (a or 0)
end

function gen_func_260(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 260, val = A + 0, ok = (b ~= nil) }
end

function gen_func_261(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 261 + B
end

function gen_func_262(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 262
  end
  return sum + (a or 0)
end

function gen_func_263(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 263, val = A + 3, ok = (b ~= nil) }
end

function gen_func_264(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 264 + B
end

function gen_func_265(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 265
  end
  return sum + (a or 0)
end

function gen_func_266(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 266, val = A + 6, ok = (b ~= nil) }
end

function gen_func_267(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 267 + B
end

function gen_func_268(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 268
  end
  return sum + (a or 0)
end

function gen_func_269(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 269, val = A + 9, ok = (b ~= nil) }
end

function gen_func_270(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 270 + B
end

function gen_func_271(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 271
  end
  return sum + (a or 0)
end

function gen_func_272(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 272, val = A + 2, ok = (b ~= nil) }
end

function gen_func_273(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 273 + B
end

function gen_func_274(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 274
  end
  return sum + (a or 0)
end

function gen_func_275(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 275, val = A + 5, ok = (b ~= nil) }
end

function gen_func_276(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 276 + B
end

function gen_func_277(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 277
  end
  return sum + (a or 0)
end

function gen_func_278(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 278, val = A + 8, ok = (b ~= nil) }
end

function gen_func_279(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 279 + B
end

function gen_func_280(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 280
  end
  return sum + (a or 0)
end

function gen_func_281(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 281, val = A + 1, ok = (b ~= nil) }
end

function gen_func_282(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 282 + B
end

function gen_func_283(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 283
  end
  return sum + (a or 0)
end

function gen_func_284(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 284, val = A + 4, ok = (b ~= nil) }
end

function gen_func_285(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 285 + B
end

function gen_func_286(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 286
  end
  return sum + (a or 0)
end

function gen_func_287(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 287, val = A + 7, ok = (b ~= nil) }
end

function gen_func_288(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 288 + B
end

function gen_func_289(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 289
  end
  return sum + (a or 0)
end

function gen_func_290(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 290, val = A + 0, ok = (b ~= nil) }
end

function gen_func_291(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 291 + B
end

function gen_func_292(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 292
  end
  return sum + (a or 0)
end

function gen_func_293(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 293, val = A + 3, ok = (b ~= nil) }
end

function gen_func_294(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 294 + B
end

function gen_func_295(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 295
  end
  return sum + (a or 0)
end

function gen_func_296(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 296, val = A + 6, ok = (b ~= nil) }
end

function gen_func_297(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 297 + B
end

function gen_func_298(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 298
  end
  return sum + (a or 0)
end

function gen_func_299(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 299, val = A + 9, ok = (b ~= nil) }
end

function gen_func_300(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 300 + B
end

function gen_func_301(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 301
  end
  return sum + (a or 0)
end

function gen_func_302(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 302, val = A + 2, ok = (b ~= nil) }
end

function gen_func_303(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 303 + B
end

function gen_func_304(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 304
  end
  return sum + (a or 0)
end

function gen_func_305(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 305, val = A + 5, ok = (b ~= nil) }
end

function gen_func_306(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 306 + B
end

function gen_func_307(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 307
  end
  return sum + (a or 0)
end

function gen_func_308(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 308, val = A + 8, ok = (b ~= nil) }
end

function gen_func_309(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 309 + B
end

function gen_func_310(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 310
  end
  return sum + (a or 0)
end

function gen_func_311(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 311, val = A + 1, ok = (b ~= nil) }
end

function gen_func_312(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 312 + B
end

function gen_func_313(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 313
  end
  return sum + (a or 0)
end

function gen_func_314(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 314, val = A + 4, ok = (b ~= nil) }
end

function gen_func_315(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 315 + B
end

function gen_func_316(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 316
  end
  return sum + (a or 0)
end

function gen_func_317(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 317, val = A + 7, ok = (b ~= nil) }
end

function gen_func_318(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 318 + B
end

function gen_func_319(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 319
  end
  return sum + (a or 0)
end

function gen_func_320(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 320, val = A + 0, ok = (b ~= nil) }
end

function gen_func_321(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 321 + B
end

function gen_func_322(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 322
  end
  return sum + (a or 0)
end

function gen_func_323(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 323, val = A + 3, ok = (b ~= nil) }
end

function gen_func_324(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 324 + B
end

function gen_func_325(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 325
  end
  return sum + (a or 0)
end

function gen_func_326(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 326, val = A + 6, ok = (b ~= nil) }
end

function gen_func_327(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 327 + B
end

function gen_func_328(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 328
  end
  return sum + (a or 0)
end

function gen_func_329(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 329, val = A + 9, ok = (b ~= nil) }
end

function gen_func_330(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 330 + B
end

function gen_func_331(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 331
  end
  return sum + (a or 0)
end

function gen_func_332(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 332, val = A + 2, ok = (b ~= nil) }
end

function gen_func_333(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 333 + B
end

function gen_func_334(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 334
  end
  return sum + (a or 0)
end

function gen_func_335(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 335, val = A + 5, ok = (b ~= nil) }
end

function gen_func_336(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 336 + B
end

function gen_func_337(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 337
  end
  return sum + (a or 0)
end

function gen_func_338(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 338, val = A + 8, ok = (b ~= nil) }
end

function gen_func_339(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 339 + B
end

function gen_func_340(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 340
  end
  return sum + (a or 0)
end

function gen_func_341(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 341, val = A + 1, ok = (b ~= nil) }
end

function gen_func_342(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 342 + B
end

function gen_func_343(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 343
  end
  return sum + (a or 0)
end

function gen_func_344(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 344, val = A + 4, ok = (b ~= nil) }
end

function gen_func_345(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 345 + B
end

function gen_func_346(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 346
  end
  return sum + (a or 0)
end

function gen_func_347(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 347, val = A + 7, ok = (b ~= nil) }
end

function gen_func_348(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 348 + B
end

function gen_func_349(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 349
  end
  return sum + (a or 0)
end

function gen_func_350(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 350, val = A + 0, ok = (b ~= nil) }
end

function gen_func_351(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 351 + B
end

function gen_func_352(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 352
  end
  return sum + (a or 0)
end

function gen_func_353(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 353, val = A + 3, ok = (b ~= nil) }
end

function gen_func_354(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 354 + B
end

function gen_func_355(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 355
  end
  return sum + (a or 0)
end

function gen_func_356(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 356, val = A + 6, ok = (b ~= nil) }
end

function gen_func_357(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 357 + B
end

function gen_func_358(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 358
  end
  return sum + (a or 0)
end

function gen_func_359(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 359, val = A + 9, ok = (b ~= nil) }
end

function gen_func_360(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 360 + B
end

function gen_func_361(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 361
  end
  return sum + (a or 0)
end

function gen_func_362(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 362, val = A + 2, ok = (b ~= nil) }
end

function gen_func_363(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 363 + B
end

function gen_func_364(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 364
  end
  return sum + (a or 0)
end

function gen_func_365(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 365, val = A + 5, ok = (b ~= nil) }
end

function gen_func_366(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 366 + B
end

function gen_func_367(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 367
  end
  return sum + (a or 0)
end

function gen_func_368(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 368, val = A + 8, ok = (b ~= nil) }
end

function gen_func_369(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 369 + B
end

function gen_func_370(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 370
  end
  return sum + (a or 0)
end

function gen_func_371(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 371, val = A + 1, ok = (b ~= nil) }
end

function gen_func_372(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 372 + B
end

function gen_func_373(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 373
  end
  return sum + (a or 0)
end

function gen_func_374(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 374, val = A + 4, ok = (b ~= nil) }
end

function gen_func_375(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 375 + B
end

function gen_func_376(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 376
  end
  return sum + (a or 0)
end

function gen_func_377(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 377, val = A + 7, ok = (b ~= nil) }
end

function gen_func_378(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 378 + B
end

function gen_func_379(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 379
  end
  return sum + (a or 0)
end

function gen_func_380(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 380, val = A + 0, ok = (b ~= nil) }
end

function gen_func_381(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 381 + B
end

function gen_func_382(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 382
  end
  return sum + (a or 0)
end

function gen_func_383(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 383, val = A + 3, ok = (b ~= nil) }
end

function gen_func_384(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 384 + B
end

function gen_func_385(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 385
  end
  return sum + (a or 0)
end

function gen_func_386(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 386, val = A + 6, ok = (b ~= nil) }
end

function gen_func_387(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 387 + B
end

function gen_func_388(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 388
  end
  return sum + (a or 0)
end

function gen_func_389(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 389, val = A + 9, ok = (b ~= nil) }
end

function gen_func_390(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 390 + B
end

function gen_func_391(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 391
  end
  return sum + (a or 0)
end

function gen_func_392(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 392, val = A + 2, ok = (b ~= nil) }
end

function gen_func_393(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 393 + B
end

function gen_func_394(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 394
  end
  return sum + (a or 0)
end

function gen_func_395(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 395, val = A + 5, ok = (b ~= nil) }
end

function gen_func_396(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 396 + B
end

function gen_func_397(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 397
  end
  return sum + (a or 0)
end

function gen_func_398(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 398, val = A + 8, ok = (b ~= nil) }
end

function gen_func_399(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 399 + B
end

function gen_func_400(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 400
  end
  return sum + (a or 0)
end

function gen_func_401(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 401, val = A + 1, ok = (b ~= nil) }
end

function gen_func_402(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 402 + B
end

function gen_func_403(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 403
  end
  return sum + (a or 0)
end

function gen_func_404(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 404, val = A + 4, ok = (b ~= nil) }
end

function gen_func_405(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 405 + B
end

function gen_func_406(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 406
  end
  return sum + (a or 0)
end

function gen_func_407(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 407, val = A + 7, ok = (b ~= nil) }
end

function gen_func_408(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 408 + B
end

function gen_func_409(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 409
  end
  return sum + (a or 0)
end

function gen_func_410(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 410, val = A + 0, ok = (b ~= nil) }
end

function gen_func_411(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 411 + B
end

function gen_func_412(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 412
  end
  return sum + (a or 0)
end

function gen_func_413(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 413, val = A + 3, ok = (b ~= nil) }
end

function gen_func_414(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 414 + B
end

function gen_func_415(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 415
  end
  return sum + (a or 0)
end

function gen_func_416(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 416, val = A + 6, ok = (b ~= nil) }
end

function gen_func_417(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 417 + B
end

function gen_func_418(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 418
  end
  return sum + (a or 0)
end

function gen_func_419(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 419, val = A + 9, ok = (b ~= nil) }
end

function gen_func_420(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 420 + B
end

function gen_func_421(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 421
  end
  return sum + (a or 0)
end

function gen_func_422(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 422, val = A + 2, ok = (b ~= nil) }
end

function gen_func_423(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 423 + B
end

function gen_func_424(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 424
  end
  return sum + (a or 0)
end

function gen_func_425(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 425, val = A + 5, ok = (b ~= nil) }
end

function gen_func_426(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 426 + B
end

function gen_func_427(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 427
  end
  return sum + (a or 0)
end

function gen_func_428(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 428, val = A + 8, ok = (b ~= nil) }
end

function gen_func_429(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 429 + B
end

function gen_func_430(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 430
  end
  return sum + (a or 0)
end

function gen_func_431(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 431, val = A + 1, ok = (b ~= nil) }
end

function gen_func_432(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 432 + B
end

function gen_func_433(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 433
  end
  return sum + (a or 0)
end

function gen_func_434(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 434, val = A + 4, ok = (b ~= nil) }
end

function gen_func_435(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 435 + B
end

function gen_func_436(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 436
  end
  return sum + (a or 0)
end

function gen_func_437(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 437, val = A + 7, ok = (b ~= nil) }
end

function gen_func_438(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 438 + B
end

function gen_func_439(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 439
  end
  return sum + (a or 0)
end

function gen_func_440(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 440, val = A + 0, ok = (b ~= nil) }
end

function gen_func_441(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 441 + B
end

function gen_func_442(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 442
  end
  return sum + (a or 0)
end

function gen_func_443(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 443, val = A + 3, ok = (b ~= nil) }
end

function gen_func_444(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 444 + B
end

function gen_func_445(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 445
  end
  return sum + (a or 0)
end

function gen_func_446(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 446, val = A + 6, ok = (b ~= nil) }
end

function gen_func_447(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 447 + B
end

function gen_func_448(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 448
  end
  return sum + (a or 0)
end

function gen_func_449(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 449, val = A + 9, ok = (b ~= nil) }
end

function gen_func_450(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 450 + B
end

function gen_func_451(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 451
  end
  return sum + (a or 0)
end

function gen_func_452(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 452, val = A + 2, ok = (b ~= nil) }
end

function gen_func_453(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 453 + B
end

function gen_func_454(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 454
  end
  return sum + (a or 0)
end

function gen_func_455(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 455, val = A + 5, ok = (b ~= nil) }
end

function gen_func_456(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 456 + B
end

function gen_func_457(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 457
  end
  return sum + (a or 0)
end

function gen_func_458(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 458, val = A + 8, ok = (b ~= nil) }
end

function gen_func_459(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 459 + B
end

function gen_func_460(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 460
  end
  return sum + (a or 0)
end

function gen_func_461(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 461, val = A + 1, ok = (b ~= nil) }
end

function gen_func_462(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 462 + B
end

function gen_func_463(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 463
  end
  return sum + (a or 0)
end

function gen_func_464(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 464, val = A + 4, ok = (b ~= nil) }
end

function gen_func_465(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 465 + B
end

function gen_func_466(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 466
  end
  return sum + (a or 0)
end

function gen_func_467(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 467, val = A + 7, ok = (b ~= nil) }
end

function gen_func_468(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 468 + B
end

function gen_func_469(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 469
  end
  return sum + (a or 0)
end

function gen_func_470(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 470, val = A + 0, ok = (b ~= nil) }
end

function gen_func_471(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 471 + B
end

function gen_func_472(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 472
  end
  return sum + (a or 0)
end

function gen_func_473(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 473, val = A + 3, ok = (b ~= nil) }
end

function gen_func_474(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 474 + B
end

function gen_func_475(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 475
  end
  return sum + (a or 0)
end

function gen_func_476(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 476, val = A + 6, ok = (b ~= nil) }
end

function gen_func_477(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 477 + B
end

function gen_func_478(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 478
  end
  return sum + (a or 0)
end

function gen_func_479(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 479, val = A + 9, ok = (b ~= nil) }
end

function gen_func_480(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 480 + B
end

function gen_func_481(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 481
  end
  return sum + (a or 0)
end

function gen_func_482(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 482, val = A + 2, ok = (b ~= nil) }
end

function gen_func_483(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 483 + B
end

function gen_func_484(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 484
  end
  return sum + (a or 0)
end

function gen_func_485(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 485, val = A + 5, ok = (b ~= nil) }
end

function gen_func_486(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 486 + B
end

function gen_func_487(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 487
  end
  return sum + (a or 0)
end

function gen_func_488(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 488, val = A + 8, ok = (b ~= nil) }
end

function gen_func_489(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 489 + B
end

function gen_func_490(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 490
  end
  return sum + (a or 0)
end

function gen_func_491(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 491, val = A + 1, ok = (b ~= nil) }
end

function gen_func_492(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 492 + B
end

function gen_func_493(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 493
  end
  return sum + (a or 0)
end

function gen_func_494(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 494, val = A + 4, ok = (b ~= nil) }
end

function gen_func_495(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 495 + B
end

function gen_func_496(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 496
  end
  return sum + (a or 0)
end

function gen_func_497(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 497, val = A + 7, ok = (b ~= nil) }
end

function gen_func_498(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 498 + B
end

function gen_func_499(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 499
  end
  return sum + (a or 0)
end

function gen_func_500(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 500, val = A + 0, ok = (b ~= nil) }
end

function gen_func_501(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 501 + B
end

function gen_func_502(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 502
  end
  return sum + (a or 0)
end

function gen_func_503(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 503, val = A + 3, ok = (b ~= nil) }
end

function gen_func_504(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 504 + B
end

function gen_func_505(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 505
  end
  return sum + (a or 0)
end

function gen_func_506(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 506, val = A + 6, ok = (b ~= nil) }
end

function gen_func_507(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 507 + B
end

function gen_func_508(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 508
  end
  return sum + (a or 0)
end

function gen_func_509(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 509, val = A + 9, ok = (b ~= nil) }
end

function gen_func_510(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 510 + B
end

function gen_func_511(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 511
  end
  return sum + (a or 0)
end

function gen_func_512(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 512, val = A + 2, ok = (b ~= nil) }
end

function gen_func_513(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 513 + B
end

function gen_func_514(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 514
  end
  return sum + (a or 0)
end

function gen_func_515(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 515, val = A + 5, ok = (b ~= nil) }
end

function gen_func_516(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 516 + B
end

function gen_func_517(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 517
  end
  return sum + (a or 0)
end

function gen_func_518(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 518, val = A + 8, ok = (b ~= nil) }
end

function gen_func_519(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 519 + B
end

function gen_func_520(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 520
  end
  return sum + (a or 0)
end

function gen_func_521(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 521, val = A + 1, ok = (b ~= nil) }
end

function gen_func_522(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 522 + B
end

function gen_func_523(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 523
  end
  return sum + (a or 0)
end

function gen_func_524(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 524, val = A + 4, ok = (b ~= nil) }
end

function gen_func_525(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 525 + B
end

function gen_func_526(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 526
  end
  return sum + (a or 0)
end

function gen_func_527(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 527, val = A + 7, ok = (b ~= nil) }
end

function gen_func_528(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 528 + B
end

function gen_func_529(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 529
  end
  return sum + (a or 0)
end

function gen_func_530(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 530, val = A + 0, ok = (b ~= nil) }
end

function gen_func_531(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 531 + B
end

function gen_func_532(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 532
  end
  return sum + (a or 0)
end

function gen_func_533(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 533, val = A + 3, ok = (b ~= nil) }
end

function gen_func_534(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 534 + B
end

function gen_func_535(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 535
  end
  return sum + (a or 0)
end

function gen_func_536(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 536, val = A + 6, ok = (b ~= nil) }
end

function gen_func_537(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 537 + B
end

function gen_func_538(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 538
  end
  return sum + (a or 0)
end

function gen_func_539(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 539, val = A + 9, ok = (b ~= nil) }
end

function gen_func_540(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 540 + B
end

function gen_func_541(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 541
  end
  return sum + (a or 0)
end

function gen_func_542(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 542, val = A + 2, ok = (b ~= nil) }
end

function gen_func_543(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 543 + B
end

function gen_func_544(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 544
  end
  return sum + (a or 0)
end

function gen_func_545(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 545, val = A + 5, ok = (b ~= nil) }
end

function gen_func_546(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 546 + B
end

function gen_func_547(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 547
  end
  return sum + (a or 0)
end

function gen_func_548(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 548, val = A + 8, ok = (b ~= nil) }
end

function gen_func_549(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 549 + B
end

function gen_func_550(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 550
  end
  return sum + (a or 0)
end

function gen_func_551(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 551, val = A + 1, ok = (b ~= nil) }
end

function gen_func_552(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 552 + B
end

function gen_func_553(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 553
  end
  return sum + (a or 0)
end

function gen_func_554(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 554, val = A + 4, ok = (b ~= nil) }
end

function gen_func_555(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 555 + B
end

function gen_func_556(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 556
  end
  return sum + (a or 0)
end

function gen_func_557(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 557, val = A + 7, ok = (b ~= nil) }
end

function gen_func_558(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 558 + B
end

function gen_func_559(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 559
  end
  return sum + (a or 0)
end

function gen_func_560(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 560, val = A + 0, ok = (b ~= nil) }
end

function gen_func_561(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 561 + B
end

function gen_func_562(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 562
  end
  return sum + (a or 0)
end

function gen_func_563(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 563, val = A + 3, ok = (b ~= nil) }
end

function gen_func_564(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 564 + B
end

function gen_func_565(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 565
  end
  return sum + (a or 0)
end

function gen_func_566(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 566, val = A + 6, ok = (b ~= nil) }
end

function gen_func_567(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 567 + B
end

function gen_func_568(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 568
  end
  return sum + (a or 0)
end

function gen_func_569(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 569, val = A + 9, ok = (b ~= nil) }
end

function gen_func_570(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 570 + B
end

function gen_func_571(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 571
  end
  return sum + (a or 0)
end

function gen_func_572(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 572, val = A + 2, ok = (b ~= nil) }
end

function gen_func_573(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 573 + B
end

function gen_func_574(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 574
  end
  return sum + (a or 0)
end

function gen_func_575(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 575, val = A + 5, ok = (b ~= nil) }
end

function gen_func_576(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 576 + B
end

function gen_func_577(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 577
  end
  return sum + (a or 0)
end

function gen_func_578(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 578, val = A + 8, ok = (b ~= nil) }
end

function gen_func_579(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 579 + B
end

function gen_func_580(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 580
  end
  return sum + (a or 0)
end

function gen_func_581(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 581, val = A + 1, ok = (b ~= nil) }
end

function gen_func_582(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 582 + B
end

function gen_func_583(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 583
  end
  return sum + (a or 0)
end

function gen_func_584(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 584, val = A + 4, ok = (b ~= nil) }
end

function gen_func_585(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 585 + B
end

function gen_func_586(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 586
  end
  return sum + (a or 0)
end

function gen_func_587(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 587, val = A + 7, ok = (b ~= nil) }
end

function gen_func_588(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 588 + B
end

function gen_func_589(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 589
  end
  return sum + (a or 0)
end

function gen_func_590(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 590, val = A + 0, ok = (b ~= nil) }
end

function gen_func_591(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 591 + B
end

function gen_func_592(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 592
  end
  return sum + (a or 0)
end

function gen_func_593(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 593, val = A + 3, ok = (b ~= nil) }
end

function gen_func_594(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 594 + B
end

function gen_func_595(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 595
  end
  return sum + (a or 0)
end

function gen_func_596(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 596, val = A + 6, ok = (b ~= nil) }
end

function gen_func_597(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 597 + B
end

function gen_func_598(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 598
  end
  return sum + (a or 0)
end

function gen_func_599(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 599, val = A + 9, ok = (b ~= nil) }
end

function gen_func_600(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 600 + B
end

function gen_func_601(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 601
  end
  return sum + (a or 0)
end

function gen_func_602(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 602, val = A + 2, ok = (b ~= nil) }
end

function gen_func_603(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 603 + B
end

function gen_func_604(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 604
  end
  return sum + (a or 0)
end

function gen_func_605(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 605, val = A + 5, ok = (b ~= nil) }
end

function gen_func_606(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 606 + B
end

function gen_func_607(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 607
  end
  return sum + (a or 0)
end

function gen_func_608(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 608, val = A + 8, ok = (b ~= nil) }
end

function gen_func_609(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 609 + B
end

function gen_func_610(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 610
  end
  return sum + (a or 0)
end

function gen_func_611(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 611, val = A + 1, ok = (b ~= nil) }
end

function gen_func_612(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 612 + B
end

function gen_func_613(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 613
  end
  return sum + (a or 0)
end

function gen_func_614(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 614, val = A + 4, ok = (b ~= nil) }
end

function gen_func_615(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 615 + B
end

function gen_func_616(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 616
  end
  return sum + (a or 0)
end

function gen_func_617(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 617, val = A + 7, ok = (b ~= nil) }
end

function gen_func_618(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 618 + B
end

function gen_func_619(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 619
  end
  return sum + (a or 0)
end

function gen_func_620(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 620, val = A + 0, ok = (b ~= nil) }
end

function gen_func_621(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 621 + B
end

function gen_func_622(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 622
  end
  return sum + (a or 0)
end

function gen_func_623(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 623, val = A + 3, ok = (b ~= nil) }
end

function gen_func_624(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 624 + B
end

function gen_func_625(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 625
  end
  return sum + (a or 0)
end

function gen_func_626(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 626, val = A + 6, ok = (b ~= nil) }
end

function gen_func_627(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 627 + B
end

function gen_func_628(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 628
  end
  return sum + (a or 0)
end

function gen_func_629(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 629, val = A + 9, ok = (b ~= nil) }
end

function gen_func_630(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 630 + B
end

function gen_func_631(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 631
  end
  return sum + (a or 0)
end

function gen_func_632(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 632, val = A + 2, ok = (b ~= nil) }
end

function gen_func_633(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 633 + B
end

function gen_func_634(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 634
  end
  return sum + (a or 0)
end

function gen_func_635(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 635, val = A + 5, ok = (b ~= nil) }
end

function gen_func_636(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 636 + B
end

function gen_func_637(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 637
  end
  return sum + (a or 0)
end

function gen_func_638(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 638, val = A + 8, ok = (b ~= nil) }
end

function gen_func_639(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 639 + B
end

function gen_func_640(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 640
  end
  return sum + (a or 0)
end

function gen_func_641(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 641, val = A + 1, ok = (b ~= nil) }
end

function gen_func_642(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 642 + B
end

function gen_func_643(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 643
  end
  return sum + (a or 0)
end

function gen_func_644(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 644, val = A + 4, ok = (b ~= nil) }
end

function gen_func_645(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 645 + B
end

function gen_func_646(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 646
  end
  return sum + (a or 0)
end

function gen_func_647(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 647, val = A + 7, ok = (b ~= nil) }
end

function gen_func_648(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 648 + B
end

function gen_func_649(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 649
  end
  return sum + (a or 0)
end

function gen_func_650(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 650, val = A + 0, ok = (b ~= nil) }
end

function gen_func_651(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 651 + B
end

function gen_func_652(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 652
  end
  return sum + (a or 0)
end

function gen_func_653(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 653, val = A + 3, ok = (b ~= nil) }
end

function gen_func_654(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 654 + B
end

function gen_func_655(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 655
  end
  return sum + (a or 0)
end

function gen_func_656(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 656, val = A + 6, ok = (b ~= nil) }
end

function gen_func_657(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 657 + B
end

function gen_func_658(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 658
  end
  return sum + (a or 0)
end

function gen_func_659(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 659, val = A + 9, ok = (b ~= nil) }
end

function gen_func_660(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 660 + B
end

function gen_func_661(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 661
  end
  return sum + (a or 0)
end

function gen_func_662(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 662, val = A + 2, ok = (b ~= nil) }
end

function gen_func_663(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 663 + B
end

function gen_func_664(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 664
  end
  return sum + (a or 0)
end

function gen_func_665(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 665, val = A + 5, ok = (b ~= nil) }
end

function gen_func_666(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 666 + B
end

function gen_func_667(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 667
  end
  return sum + (a or 0)
end

function gen_func_668(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 668, val = A + 8, ok = (b ~= nil) }
end

function gen_func_669(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 669 + B
end

function gen_func_670(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 670
  end
  return sum + (a or 0)
end

function gen_func_671(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 671, val = A + 1, ok = (b ~= nil) }
end

function gen_func_672(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 672 + B
end

function gen_func_673(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 673
  end
  return sum + (a or 0)
end

function gen_func_674(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 674, val = A + 4, ok = (b ~= nil) }
end

function gen_func_675(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 675 + B
end

function gen_func_676(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 676
  end
  return sum + (a or 0)
end

function gen_func_677(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 677, val = A + 7, ok = (b ~= nil) }
end

function gen_func_678(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 678 + B
end

function gen_func_679(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 679
  end
  return sum + (a or 0)
end

function gen_func_680(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 680, val = A + 0, ok = (b ~= nil) }
end

function gen_func_681(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 681 + B
end

function gen_func_682(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 682
  end
  return sum + (a or 0)
end

function gen_func_683(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 683, val = A + 3, ok = (b ~= nil) }
end

function gen_func_684(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 684 + B
end

function gen_func_685(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 685
  end
  return sum + (a or 0)
end

function gen_func_686(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 686, val = A + 6, ok = (b ~= nil) }
end

function gen_func_687(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 687 + B
end

function gen_func_688(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 688
  end
  return sum + (a or 0)
end

function gen_func_689(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 689, val = A + 9, ok = (b ~= nil) }
end

function gen_func_690(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 690 + B
end

function gen_func_691(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 691
  end
  return sum + (a or 0)
end

function gen_func_692(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 692, val = A + 2, ok = (b ~= nil) }
end

function gen_func_693(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 693 + B
end

function gen_func_694(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 694
  end
  return sum + (a or 0)
end

function gen_func_695(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 695, val = A + 5, ok = (b ~= nil) }
end

function gen_func_696(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 696 + B
end

function gen_func_697(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 697
  end
  return sum + (a or 0)
end

function gen_func_698(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 698, val = A + 8, ok = (b ~= nil) }
end

function gen_func_699(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 699 + B
end

function gen_func_700(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 700
  end
  return sum + (a or 0)
end

function gen_func_701(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 701, val = A + 1, ok = (b ~= nil) }
end

function gen_func_702(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 702 + B
end

function gen_func_703(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 703
  end
  return sum + (a or 0)
end

function gen_func_704(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 704, val = A + 4, ok = (b ~= nil) }
end

function gen_func_705(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 705 + B
end

function gen_func_706(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 706
  end
  return sum + (a or 0)
end

function gen_func_707(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 707, val = A + 7, ok = (b ~= nil) }
end

function gen_func_708(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 708 + B
end

function gen_func_709(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 709
  end
  return sum + (a or 0)
end

function gen_func_710(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 710, val = A + 0, ok = (b ~= nil) }
end

function gen_func_711(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 711 + B
end

function gen_func_712(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 712
  end
  return sum + (a or 0)
end

function gen_func_713(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 713, val = A + 3, ok = (b ~= nil) }
end

function gen_func_714(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 714 + B
end

function gen_func_715(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 715
  end
  return sum + (a or 0)
end

function gen_func_716(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 716, val = A + 6, ok = (b ~= nil) }
end

function gen_func_717(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 717 + B
end

function gen_func_718(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 718
  end
  return sum + (a or 0)
end

function gen_func_719(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 719, val = A + 9, ok = (b ~= nil) }
end

function gen_func_720(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 720 + B
end

function gen_func_721(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 721
  end
  return sum + (a or 0)
end

function gen_func_722(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 722, val = A + 2, ok = (b ~= nil) }
end

function gen_func_723(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 723 + B
end

function gen_func_724(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 724
  end
  return sum + (a or 0)
end

function gen_func_725(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 725, val = A + 5, ok = (b ~= nil) }
end

function gen_func_726(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 726 + B
end

function gen_func_727(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 727
  end
  return sum + (a or 0)
end

function gen_func_728(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 728, val = A + 8, ok = (b ~= nil) }
end

function gen_func_729(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 729 + B
end

function gen_func_730(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 730
  end
  return sum + (a or 0)
end

function gen_func_731(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 731, val = A + 1, ok = (b ~= nil) }
end

function gen_func_732(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 732 + B
end

function gen_func_733(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 733
  end
  return sum + (a or 0)
end

function gen_func_734(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 734, val = A + 4, ok = (b ~= nil) }
end

function gen_func_735(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 735 + B
end

function gen_func_736(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 736
  end
  return sum + (a or 0)
end

function gen_func_737(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 737, val = A + 7, ok = (b ~= nil) }
end

function gen_func_738(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 738 + B
end

function gen_func_739(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 739
  end
  return sum + (a or 0)
end

function gen_func_740(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 740, val = A + 0, ok = (b ~= nil) }
end

function gen_func_741(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 741 + B
end

function gen_func_742(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 742
  end
  return sum + (a or 0)
end

function gen_func_743(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 743, val = A + 3, ok = (b ~= nil) }
end

function gen_func_744(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 744 + B
end

function gen_func_745(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 745
  end
  return sum + (a or 0)
end

function gen_func_746(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 746, val = A + 6, ok = (b ~= nil) }
end

function gen_func_747(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 747 + B
end

function gen_func_748(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 748
  end
  return sum + (a or 0)
end

function gen_func_749(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 749, val = A + 9, ok = (b ~= nil) }
end

function gen_func_750(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 750 + B
end

function gen_func_751(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 751
  end
  return sum + (a or 0)
end

function gen_func_752(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 752, val = A + 2, ok = (b ~= nil) }
end

function gen_func_753(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 753 + B
end

function gen_func_754(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 754
  end
  return sum + (a or 0)
end

function gen_func_755(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 755, val = A + 5, ok = (b ~= nil) }
end

function gen_func_756(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 756 + B
end

function gen_func_757(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 757
  end
  return sum + (a or 0)
end

function gen_func_758(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 758, val = A + 8, ok = (b ~= nil) }
end

function gen_func_759(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 759 + B
end

function gen_func_760(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 760
  end
  return sum + (a or 0)
end

function gen_func_761(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 761, val = A + 1, ok = (b ~= nil) }
end

function gen_func_762(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 762 + B
end

function gen_func_763(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 763
  end
  return sum + (a or 0)
end

function gen_func_764(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 764, val = A + 4, ok = (b ~= nil) }
end

function gen_func_765(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 765 + B
end

function gen_func_766(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 766
  end
  return sum + (a or 0)
end

function gen_func_767(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 767, val = A + 7, ok = (b ~= nil) }
end

function gen_func_768(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 768 + B
end

function gen_func_769(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 769
  end
  return sum + (a or 0)
end

function gen_func_770(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 770, val = A + 0, ok = (b ~= nil) }
end

function gen_func_771(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 771 + B
end

function gen_func_772(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 772
  end
  return sum + (a or 0)
end

function gen_func_773(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 773, val = A + 3, ok = (b ~= nil) }
end

function gen_func_774(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 774 + B
end

function gen_func_775(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 775
  end
  return sum + (a or 0)
end

function gen_func_776(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 776, val = A + 6, ok = (b ~= nil) }
end

function gen_func_777(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 777 + B
end

function gen_func_778(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 778
  end
  return sum + (a or 0)
end

function gen_func_779(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 779, val = A + 9, ok = (b ~= nil) }
end

function gen_func_780(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 780 + B
end

function gen_func_781(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 781
  end
  return sum + (a or 0)
end

function gen_func_782(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 782, val = A + 2, ok = (b ~= nil) }
end

function gen_func_783(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 783 + B
end

function gen_func_784(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 784
  end
  return sum + (a or 0)
end

function gen_func_785(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 785, val = A + 5, ok = (b ~= nil) }
end

function gen_func_786(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 786 + B
end

function gen_func_787(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 787
  end
  return sum + (a or 0)
end

function gen_func_788(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 788, val = A + 8, ok = (b ~= nil) }
end

function gen_func_789(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 789 + B
end

function gen_func_790(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 790
  end
  return sum + (a or 0)
end

function gen_func_791(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 791, val = A + 1, ok = (b ~= nil) }
end

function gen_func_792(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 792 + B
end

function gen_func_793(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 793
  end
  return sum + (a or 0)
end

function gen_func_794(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 794, val = A + 4, ok = (b ~= nil) }
end

function gen_func_795(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 795 + B
end

function gen_func_796(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 796
  end
  return sum + (a or 0)
end

function gen_func_797(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 797, val = A + 7, ok = (b ~= nil) }
end

function gen_func_798(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 798 + B
end

function gen_func_799(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 799
  end
  return sum + (a or 0)
end

function gen_func_800(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 800, val = A + 0, ok = (b ~= nil) }
end

function gen_func_801(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 801 + B
end

function gen_func_802(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 802
  end
  return sum + (a or 0)
end

function gen_func_803(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 803, val = A + 3, ok = (b ~= nil) }
end

function gen_func_804(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 804 + B
end

function gen_func_805(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 805
  end
  return sum + (a or 0)
end

function gen_func_806(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 806, val = A + 6, ok = (b ~= nil) }
end

function gen_func_807(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 807 + B
end

function gen_func_808(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 808
  end
  return sum + (a or 0)
end

function gen_func_809(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 809, val = A + 9, ok = (b ~= nil) }
end

function gen_func_810(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 810 + B
end

function gen_func_811(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 811
  end
  return sum + (a or 0)
end

function gen_func_812(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 812, val = A + 2, ok = (b ~= nil) }
end

function gen_func_813(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 813 + B
end

function gen_func_814(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 814
  end
  return sum + (a or 0)
end

function gen_func_815(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 815, val = A + 5, ok = (b ~= nil) }
end

function gen_func_816(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 816 + B
end

function gen_func_817(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 817
  end
  return sum + (a or 0)
end

function gen_func_818(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 818, val = A + 8, ok = (b ~= nil) }
end

function gen_func_819(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 819 + B
end

function gen_func_820(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 820
  end
  return sum + (a or 0)
end

function gen_func_821(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 821, val = A + 1, ok = (b ~= nil) }
end

function gen_func_822(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 822 + B
end

function gen_func_823(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 823
  end
  return sum + (a or 0)
end

function gen_func_824(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 824, val = A + 4, ok = (b ~= nil) }
end

function gen_func_825(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 825 + B
end

function gen_func_826(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 826
  end
  return sum + (a or 0)
end

function gen_func_827(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 827, val = A + 7, ok = (b ~= nil) }
end

function gen_func_828(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 828 + B
end

function gen_func_829(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 829
  end
  return sum + (a or 0)
end

function gen_func_830(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 830, val = A + 0, ok = (b ~= nil) }
end

function gen_func_831(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 831 + B
end

function gen_func_832(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 832
  end
  return sum + (a or 0)
end

function gen_func_833(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 833, val = A + 3, ok = (b ~= nil) }
end

function gen_func_834(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 834 + B
end

function gen_func_835(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 835
  end
  return sum + (a or 0)
end

function gen_func_836(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 836, val = A + 6, ok = (b ~= nil) }
end

function gen_func_837(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 837 + B
end

function gen_func_838(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 838
  end
  return sum + (a or 0)
end

function gen_func_839(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 839, val = A + 9, ok = (b ~= nil) }
end

function gen_func_840(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 840 + B
end

function gen_func_841(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 841
  end
  return sum + (a or 0)
end

function gen_func_842(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 842, val = A + 2, ok = (b ~= nil) }
end

function gen_func_843(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 843 + B
end

function gen_func_844(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 844
  end
  return sum + (a or 0)
end

function gen_func_845(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 845, val = A + 5, ok = (b ~= nil) }
end

function gen_func_846(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 846 + B
end

function gen_func_847(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 847
  end
  return sum + (a or 0)
end

function gen_func_848(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 848, val = A + 8, ok = (b ~= nil) }
end

function gen_func_849(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 849 + B
end

function gen_func_850(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 850
  end
  return sum + (a or 0)
end

function gen_func_851(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 851, val = A + 1, ok = (b ~= nil) }
end

function gen_func_852(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 852 + B
end

function gen_func_853(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 853
  end
  return sum + (a or 0)
end

function gen_func_854(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 854, val = A + 4, ok = (b ~= nil) }
end

function gen_func_855(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 855 + B
end

function gen_func_856(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 856
  end
  return sum + (a or 0)
end

function gen_func_857(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 857, val = A + 7, ok = (b ~= nil) }
end

function gen_func_858(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 858 + B
end

function gen_func_859(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 859
  end
  return sum + (a or 0)
end

function gen_func_860(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 860, val = A + 0, ok = (b ~= nil) }
end

function gen_func_861(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 861 + B
end

function gen_func_862(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 862
  end
  return sum + (a or 0)
end

function gen_func_863(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 863, val = A + 3, ok = (b ~= nil) }
end

function gen_func_864(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 864 + B
end

function gen_func_865(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 865
  end
  return sum + (a or 0)
end

function gen_func_866(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 866, val = A + 6, ok = (b ~= nil) }
end

function gen_func_867(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 867 + B
end

function gen_func_868(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 868
  end
  return sum + (a or 0)
end

function gen_func_869(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 869, val = A + 9, ok = (b ~= nil) }
end

function gen_func_870(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 870 + B
end

function gen_func_871(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 871
  end
  return sum + (a or 0)
end

function gen_func_872(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 872, val = A + 2, ok = (b ~= nil) }
end

function gen_func_873(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 873 + B
end

function gen_func_874(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 874
  end
  return sum + (a or 0)
end

function gen_func_875(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 875, val = A + 5, ok = (b ~= nil) }
end

function gen_func_876(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 876 + B
end

function gen_func_877(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 877
  end
  return sum + (a or 0)
end

function gen_func_878(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 878, val = A + 8, ok = (b ~= nil) }
end

function gen_func_879(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 879 + B
end

function gen_func_880(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 880
  end
  return sum + (a or 0)
end

function gen_func_881(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 881, val = A + 1, ok = (b ~= nil) }
end

function gen_func_882(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 882 + B
end

function gen_func_883(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 883
  end
  return sum + (a or 0)
end

function gen_func_884(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 884, val = A + 4, ok = (b ~= nil) }
end

function gen_func_885(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 885 + B
end

function gen_func_886(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 886
  end
  return sum + (a or 0)
end

function gen_func_887(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 887, val = A + 7, ok = (b ~= nil) }
end

function gen_func_888(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 888 + B
end

function gen_func_889(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 889
  end
  return sum + (a or 0)
end

function gen_func_890(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 890, val = A + 0, ok = (b ~= nil) }
end

function gen_func_891(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 891 + B
end

function gen_func_892(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 892
  end
  return sum + (a or 0)
end

function gen_func_893(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 893, val = A + 3, ok = (b ~= nil) }
end

function gen_func_894(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 894 + B
end

function gen_func_895(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 895
  end
  return sum + (a or 0)
end

function gen_func_896(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 896, val = A + 6, ok = (b ~= nil) }
end

function gen_func_897(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 897 + B
end

function gen_func_898(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 898
  end
  return sum + (a or 0)
end

function gen_func_899(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 899, val = A + 9, ok = (b ~= nil) }
end

function gen_func_900(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 900 + B
end

function gen_func_901(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 901
  end
  return sum + (a or 0)
end

function gen_func_902(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 902, val = A + 2, ok = (b ~= nil) }
end

function gen_func_903(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 903 + B
end

function gen_func_904(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 904
  end
  return sum + (a or 0)
end

function gen_func_905(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 905, val = A + 5, ok = (b ~= nil) }
end

function gen_func_906(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 906 + B
end

function gen_func_907(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 907
  end
  return sum + (a or 0)
end

function gen_func_908(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 908, val = A + 8, ok = (b ~= nil) }
end

function gen_func_909(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 909 + B
end

function gen_func_910(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 910
  end
  return sum + (a or 0)
end

function gen_func_911(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 911, val = A + 1, ok = (b ~= nil) }
end

function gen_func_912(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 912 + B
end

function gen_func_913(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 913
  end
  return sum + (a or 0)
end

function gen_func_914(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 914, val = A + 4, ok = (b ~= nil) }
end

function gen_func_915(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 915 + B
end

function gen_func_916(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 916
  end
  return sum + (a or 0)
end

function gen_func_917(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 917, val = A + 7, ok = (b ~= nil) }
end

function gen_func_918(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 918 + B
end

function gen_func_919(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 919
  end
  return sum + (a or 0)
end

function gen_func_920(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 920, val = A + 0, ok = (b ~= nil) }
end

function gen_func_921(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 921 + B
end

function gen_func_922(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 922
  end
  return sum + (a or 0)
end

function gen_func_923(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 923, val = A + 3, ok = (b ~= nil) }
end

function gen_func_924(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 924 + B
end

function gen_func_925(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 925
  end
  return sum + (a or 0)
end

function gen_func_926(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 926, val = A + 6, ok = (b ~= nil) }
end

function gen_func_927(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 927 + B
end

function gen_func_928(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 928
  end
  return sum + (a or 0)
end

function gen_func_929(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 929, val = A + 9, ok = (b ~= nil) }
end

function gen_func_930(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 930 + B
end

function gen_func_931(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 931
  end
  return sum + (a or 0)
end

function gen_func_932(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 932, val = A + 2, ok = (b ~= nil) }
end

function gen_func_933(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 933 + B
end

function gen_func_934(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 934
  end
  return sum + (a or 0)
end

function gen_func_935(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 935, val = A + 5, ok = (b ~= nil) }
end

function gen_func_936(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 936 + B
end

function gen_func_937(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 937
  end
  return sum + (a or 0)
end

function gen_func_938(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 938, val = A + 8, ok = (b ~= nil) }
end

function gen_func_939(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 939 + B
end

function gen_func_940(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 940
  end
  return sum + (a or 0)
end

function gen_func_941(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 941, val = A + 1, ok = (b ~= nil) }
end

function gen_func_942(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 942 + B
end

function gen_func_943(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 943
  end
  return sum + (a or 0)
end

function gen_func_944(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 944, val = A + 4, ok = (b ~= nil) }
end

function gen_func_945(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 945 + B
end

function gen_func_946(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 946
  end
  return sum + (a or 0)
end

function gen_func_947(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 947, val = A + 7, ok = (b ~= nil) }
end

function gen_func_948(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 948 + B
end

function gen_func_949(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 949
  end
  return sum + (a or 0)
end

function gen_func_950(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 950, val = A + 0, ok = (b ~= nil) }
end

function gen_func_951(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 951 + B
end

function gen_func_952(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 952
  end
  return sum + (a or 0)
end

function gen_func_953(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 953, val = A + 3, ok = (b ~= nil) }
end

function gen_func_954(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 954 + B
end

function gen_func_955(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 955
  end
  return sum + (a or 0)
end

function gen_func_956(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 956, val = A + 6, ok = (b ~= nil) }
end

function gen_func_957(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 957 + B
end

function gen_func_958(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 958
  end
  return sum + (a or 0)
end

function gen_func_959(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 959, val = A + 9, ok = (b ~= nil) }
end

function gen_func_960(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 960 + B
end

function gen_func_961(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 961
  end
  return sum + (a or 0)
end

function gen_func_962(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 962, val = A + 2, ok = (b ~= nil) }
end

function gen_func_963(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 963 + B
end

function gen_func_964(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 964
  end
  return sum + (a or 0)
end

function gen_func_965(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 965, val = A + 5, ok = (b ~= nil) }
end

function gen_func_966(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 966 + B
end

function gen_func_967(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 967
  end
  return sum + (a or 0)
end

function gen_func_968(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 968, val = A + 8, ok = (b ~= nil) }
end

function gen_func_969(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 969 + B
end

function gen_func_970(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 970
  end
  return sum + (a or 0)
end

function gen_func_971(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 971, val = A + 1, ok = (b ~= nil) }
end

function gen_func_972(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 972 + B
end

function gen_func_973(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 973
  end
  return sum + (a or 0)
end

function gen_func_974(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 974, val = A + 4, ok = (b ~= nil) }
end

function gen_func_975(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 975 + B
end

function gen_func_976(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 976
  end
  return sum + (a or 0)
end

function gen_func_977(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 977, val = A + 7, ok = (b ~= nil) }
end

function gen_func_978(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 978 + B
end

function gen_func_979(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 979
  end
  return sum + (a or 0)
end

function gen_func_980(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 980, val = A + 0, ok = (b ~= nil) }
end

function gen_func_981(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 981 + B
end

function gen_func_982(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 982
  end
  return sum + (a or 0)
end

function gen_func_983(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 983, val = A + 3, ok = (b ~= nil) }
end

function gen_func_984(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 984 + B
end

function gen_func_985(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 985
  end
  return sum + (a or 0)
end

function gen_func_986(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 986, val = A + 6, ok = (b ~= nil) }
end

function gen_func_987(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 987 + B
end

function gen_func_988(a, b)
  -- accumulates 1..4 and adds a
  local sum = 0
  for j = 1, 4 do
    sum = sum + j * 988
  end
  return sum + (a or 0)
end

function gen_func_989(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 989, val = A + 9, ok = (b ~= nil) }
end

function gen_func_990(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 990 + B
end

function gen_func_991(a, b)
  -- accumulates 1..2 and adds a
  local sum = 0
  for j = 1, 2 do
    sum = sum + j * 991
  end
  return sum + (a or 0)
end

function gen_func_992(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 992, val = A + 2, ok = (b ~= nil) }
end

function gen_func_993(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 993 + B
end

function gen_func_994(a, b)
  -- accumulates 1..5 and adds a
  local sum = 0
  for j = 1, 5 do
    sum = sum + j * 994
  end
  return sum + (a or 0)
end

function gen_func_995(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 995, val = A + 5, ok = (b ~= nil) }
end

function gen_func_996(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 996 + B
end

function gen_func_997(a, b)
  -- accumulates 1..3 and adds a
  local sum = 0
  for j = 1, 3 do
    sum = sum + j * 997
  end
  return sum + (a or 0)
end

function gen_func_998(a, b)
  -- returns a table with computed fields
  local A = (a or 0)
  return { idx = 998, val = A + 8, ok = (b ~= nil) }
end

function gen_func_999(a, b)
  -- returns a scaled by index plus b (fallbacks to 0)
  local A = (a or 0)
  local B = (b or 0)
  return A * 999 + B
end

function gen_func_1000(a, b)
  -- accumulates 1..1 and adds a
  local sum = 0
  for j = 1, 1 do
    sum = sum + j * 1000
  end
  return sum + (a or 0)
end

